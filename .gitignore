# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Node
node_modules/
dist/

# Local data (beacon.db is the job cache; -wal/-shm are its WAL side-files)
*.sqlite3
beacon.db
beacon.db-wal
beacon.db-shm

# Background refresh log written by run.sh
.ingest.log

# Launch/close digest log written by run.sh (holds the digest text when no Telegram creds)
.digest.log

# Launcher log, when run.sh output is redirected (./run.sh > .run.log)
.run.log

# Real registry snapshots (manually refreshed); only trimmed fixtures are committed
data/registries/

# Design bundle kept locally as reference, not shipped
Design for Spec-2.zip

# Nightly SQLite backups (local only)
backups/
