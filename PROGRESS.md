# PROGRESS.md — Beacon

> Live state of the build. Update at the end of every session. Newest entries on top within each section.

## Current status

**Active slice:** 12 — Resume upload + fit scoring (**12a–12e built + suite-verified; live-key LLM run + browser sign-off pending**). Slices 0–11 remain done (8/9/10 pending their credentialed/manual steps). **12a** — pure `build_profile`/`score_match` + `ResumeProfile`/`JobFacts`/`MatchScore` value objects (`9ce0df2`; enabling vocabulary→domain refactor `7cf043c`). **12b** — resume ingest end-to-end: migration 008 (`resumes` + `job_match_scores`), `ResumeParser`/`ResumeRepo` ports, zero-dep `PlainTextResumeParser`, `SqliteResumeRepo`, `ingest_resume` (hash-dedup + active-singleton), and the `/resumes` API (POST paste / GET / PUT active / DELETE) — `94f9da4` (ingest) + `d277c22` (API). **12c** — Tier-1 heuristic scoring wired into `/jobs`: `MatchScoreRepo` port + `SqliteMatchScoreRepo` (cache in `job_match_scores`, keyed `(resume_hash, job_id)`, content_hash-gated), `application/scoring.py` (`score_jobs_for_resume` cache-gated use case + `list_scored_jobs` orchestrator + `job_facts_from_listing`), `JobRepo.get_scoring_inputs` (by-id content_hash+description fetch for the page), `/jobs?resume=<id>` attaches `match_score` (page-bounded), `sort=match` orders by fit, and `match_score` on `JobListing`/`JobOut`/`types.ts`. Default sort untouched; fit is a soft opt-in signal, never a filter. See Decisions 2026-07-15 (12c).
**Next action:** **Slice 12e is DONE** — LLM deep-match ("Assess fit") built + suite-verified, key-gated OFF by default like slice 9 (see Decisions 2026-07-16 (12e)). **Slice 12 is feature-complete.** Remaining are manual/credentialed steps only (non-blocking): (i) the **live LLM deep-match run** — set `BEACON_ANTHROPIC_API_KEY`, open a job's drawer → "Assess fit with AI", spot-check ≥5 jobs (budget-exhausted/key-absent degrades to the heuristic score with no crash), same shape as slices 8/9; (ii) the owed **12d/12e browser sign-off** via `./run.sh` (no headless browser here to screenshot, consistent with prior UI slices). Historical UI context: 12d shipped the UI (see Decisions 2026-07-15 (12d)): resume upload/paste + list/set-active/delete in Settings (`ResumePanel`), a soft **Fit badge** on job rows when a resume is active, a **Fit** option in the sort control (`?resume=`/`sort=match` wired through `api/jobs.ts`, `SortBy` gained `'match'`), and a drawer **Fit card** (overall + sub-score bars + matched/missing skill chips), all styled like the sponsorship card. 12e adds the Tier-2 rationale: `LLMMatcher` behind a new `Matcher` port, `POST /jobs/{id}/match?resume=<id>` (one budget-capped call per unseen `(resume_hash, content_hash)`, reusing the slice-9 `LLMBudget` — no second budget), stored in `job_match_scores.llm_rationale`, surfaced behind an **"Assess fit"** button in the drawer's Fit card — any LLM failure degrades silently to the heuristic (LLM is an upgrader, never a dependency). **Off until `BEACON_ANTHROPIC_API_KEY` is set**, same switch as slice 9. **Manual acceptance still owed on 12d:** a browser sign-off via `./run.sh` (upload a resume in Settings → Fit badges appear on Jobs → sort by Fit → open a job → Fit card) — no headless browser here to screenshot, consistent with prior UI slices. — **Open question for you (non-blocking, decided as paste-only for now):** the flagged **PDF-parser dep** — 12b is paste/`.txt`-only (zero-dep, SPEC's always-available path); `PdfResumeParser` is a documented one-commit drop-in behind the port. Say the word to add it (e.g. `pypdf`). Prior redesign context follows. **BEACON-2 TWO-PANE REDESIGN is committed to `main`** (`110d16c` redesign + `3444fe9` DESIGN.md rewrite) — see Decisions 2026-07-13 (Beacon-2). Working tree clean. Of the four redesign follow-ups, **commit + DESIGN.md rewrite are done**; still open: (1) visual sign-off in a browser via `./run.sh` (no headless browser here to screenshot); (2) optionally wire the static Source-health widget (44/1/2, poll 07:04) to live `/companies` health counts (TODO in `CountriesPage.tsx`, `api/companies.ts` kept for it). — **NEW: slice 12 (resume-match) added to SPEC §11 + PLAN slice 12, committed `f499528` 2026-07-14.** Two-tier design (heuristic score over all jobs, cached + page-bounded; opt-in budget-capped LLM rationale per job). **Not built** — next action to start it is PLAN 12a (pure `build_profile`/`score_match` domain, red tests first). — Separately, MVP slices 0–11 are all built (8/9/10 pending their credentialed/manual steps). Remaining are **credentialed/manual live steps only you can run** (unchanged): (A) slice-8 live Telegram send (Settings → real BotFather token/chat_id → Send test); (B) slice-9 LLM classifier is **deliberately parked/disabled for now** (key-gating — see Decisions 2026-07-11); to re-enable: set `BEACON_ANTHROPIC_API_KEY=sk-ant-…` then `uv run python scripts/spot_check_llm.py` + `uv run python -m beacon.classify --upgrade-residue`; (C) slice-10 reboot-resume — `cp deploy/com.beacon.scheduler.plist ~/Library/LaunchAgents/ && launchctl load …`, reboot, confirm `launchctl list | grep beacon` + `scheduler_started`. Optional deferred UI polish: per-job "source stale since" banner on Jobs + sidebar source-health footer + nav count badges.
**Baseline:** Slices 0–11 done (8/9/10 pending their credentialed/manual steps). `make verify` green on both stacks (**544 backend + 62 frontend** tests; +23 backend & +3 frontend in slice-12e — backend: 2 domain rationale-roundtrip + 5 LLMMatcher adapter (parse/fenced/refusal/http-error/prompt) + 4 repo cache-and-staleness + 7 deep_match_job use-case (budget/cache/degrade/single-job/unknown/content-change) + 5 API (wiring/degrade/404×2/422); frontend: 3 JobDrawer Assess-fit (button-gated, rationale render, unavailable note); prior +13 frontend in slice-12d — 3 JobsPane resume-fit + 2 JobDrawer Fit-card + 8 ResumePanel; no new migration (llm_rationale column exists since migration 008), and `vite build` clean). Manual end-to-end spot-check (real app + temp DB): a target-country registry iOS role scored 96 (skills 100, all matched), an off-strategy junior Android role 17 (skills 0, country-nudge + over-qualification penalty), no-resume rows carry `null`. New in slice 11: `SourceHealth` domain state machine (gone/schema_drift quarantine after 3, unreachable after 10; pure `record_failure`/`record_success`/`should_poll`); `SourceUnavailable` raised by `PoliteClient` (httpx→FailureKind at the HTTP door, application stays IO-free); `ingest_source` returns a failure kind (never sweeps on a failed poll — incl. all-normalize-fail=schema_drift), `ingest_all` filters quarantined + records health via `CompanyRepo.get_health/set_health`; reslug recovery is a pure CSV edit (changed ats_type/slug on re-seed resets health); weekly `probe_quarantined` restore probe; `registries_meta` now written by refresh + `stale_registries` (45-day) nag; digest gained a source-health section (`HealthAlert`/`RegistryStale`, sends even with no matches); `GET /companies/health` + the Companies view (DESIGN §3: summary cards + seed line + health table). No new migration — the health columns exist since migration 001. Live-accepted against real Greenhouse: nonsense slug → 3 polls → quarantined(gone), pre-existing job never closed, digest reports it; fix slug → health reset on re-seed → recovery poll upserted 15 real Tines jobs.

## Slice tracker

| # | Slice | Status | Done date |
|---|---|---|---|
| 0 | Skeleton & verify gate | ✅ done | 2026-07-04 |
| 1 | Greenhouse → SQLite → /jobs → JobTable | ✅ done | 2026-07-04 |
| 2 | Sponsor registries → registry_inferred | ✅ done | 2026-07-07 |
| 3 | Heuristic category/level classifier | ✅ done | 2026-07-08 |
| 4 | Lever + Ashby adapters | ✅ done | 2026-07-08 |
| 5 | Cross-source dedup | ✅ done | 2026-07-08 |
| 5.5 | Per-job user status (seen/hidden/starred) | ✅ done | 2026-07-08 |
| 6 | Explicit sponsorship text tiers | ✅ done | 2026-07-08 |
| 7 | HN Who's Hiring + JobTech adapters | ✅ done | 2026-07-08 |
| 8 | Saved searches + Telegram digest | 🟨 built + Settings-UI creds; live send is now a UI button (needs your real token) | |
| 9 | LLM fallback classifier | 🟨 built + suite-verified; **parked/disabled for now** (key-gated off, 2026-07-11) — live spot-check pending a key if re-enabled | |
| 10 | CountryPanel + RemoteOK/WWR + scheduler | 🟨 built + suite-verified; reboot-resume is a manual launchd load (your box) | |
| 11 | Source health & recovery | ✅ done | 2026-07-09 |
| 12 | Resume upload + fit scoring (heuristic all-jobs; opt-in LLM per job) | 🟨 12a–12e built + suite-verified; live-key LLM run + browser sign-off pending | |

Legend: ⬜ not started · 🟨 in progress · ✅ done (acceptance boxes checked)

## Decisions log

- **2026-07-16** — **Slice 12e built (Tier-2 LLM deep-match "Assess fit"; opt-in, key-gated OFF by default like slice 9).** Full vertical, TDD throughout. Decisions during the build: (1) **Two domain value objects** — `MatchRationale` (summary/strengths/gaps/verdict/sponsor_note) and `DeepMatchJob` (the single-job context handed to the matcher: title+description text, country, sponsor_tier, and the heuristic `MatchScore`) — with `rationale_to_json`/`rationale_from_json` for the `job_match_scores.llm_rationale` TEXT column (readable JSON, not a pickle). (2) **The Tier-2 path is SYNCHRONOUS end-to-end** (matcher, `deep_match_job`, endpoint), a **deliberate reversal of an initial async attempt**: an `async def` handler resolves the sync-generator sqlite deps in a threadpool thread and then uses that connection on the event-loop thread → SQLite's "objects created in a thread can only be used in that same thread" error (a real bug, caught by the API test). Every other handler is sync `def` for exactly this reason, and `LLMClassifier` is sync too; a sync `def` runs in the threadpool where the blocking sqlite reads **and** the blocking Anthropic call are both fine. `LLMMatcher` mirrors `LLMClassifier` (raw httpx.Client, JSON-only prompt, `raise_for_status`, tolerant outermost-`{}` parse, defaults missing fields). (3) **Reuses the slice-9 `LLMBudget`** (no second budget, per SPEC §11); `try_reserve` counts the attempt so a failing/retrying call can't blow the cap. (4) **Cache keyed `(resume_hash, content_hash)`** in `job_match_scores.llm_rationale` via new `MatchScoreRepo.get_rationale`/`set_rationale` (+ `CachedRationale` read model); a cached rationale whose content_hash still matches is reused with **no LLM call and no budget spend**. **Correctness fix:** the Tier-1 `upsert` now NULLs `llm_rationale` whenever `content_hash` changes (a `CASE` clause) — otherwise a Tier-1 re-score of an edited posting would leave a stale rationale that `get_rationale` would serve as if it were for the new content (`test_rescoring_a_changed_posting_drops_the_stale_rationale`, and end-to-end `test_changed_posting_recomputes_the_rationale`). (5) **`deep_match_job` degrades silently to heuristic-only** on `matcher=None` (no key), budget-exhausted, or ANY LLM exception — the heuristic `MatchScore` always rides along; returns `None` only for an unknown job id (→ 404). Assembles `JobFacts` from `get_job_detail` + `get_scoring_inputs`, reusing an extracted **`scoring.job_facts()`** primitive (factored out of `job_facts_from_listing` — killed the 2nd JobFacts-assembly duplication before a 3rd copy landed). (6) **`make_matcher` returns `None` without a key** (Tier 2 is LLM-only — there is no heuristic matcher, so `None` is the honest "off"; same key-as-switch as slice 9). Wired at the composition root via a **sync generator dep `get_matcher`** that owns a blocking `httpx.Client`, plus `get_llm_budget`. (7) **API `POST /jobs/{id}/match?resume=<id>`** (`resume` required → 422; unknown resume or job → 404) returns `{ match_score, rationale|null }`. (8) **Frontend:** `assessFit(id, resume)` client + a drawer **"Assess fit with AI"** button inside the Fit card, shown only when a resume is active (`AssessFit` sub-component takes a non-null `resumeId`, so no cast); on click it renders summary + a verdict pill + strengths/gaps lists + the sponsor note; a **null rationale** (no key/budget) shows an "AI deep match unavailable…" note and the heuristic Fit card always stands. `JobDrawer` gained a `resumeId` prop threaded from `JobsPane`; styled with existing tokens (DESIGN.md predates the resume feature — the CLAUDE.md frontend-design authority rule). (9) **Duplication watch (not yet acted):** the Anthropic-envelope helpers `_text_of`/`_extract_json_object` are now identical in `adapters/classify/llm.py` and `adapters/resume/llm.py` — 2nd occurrence; the repo's refactor rule acts on the 3rd, so left in place. A 3rd Anthropic adapter → extract a shared `adapters/anthropic_json.py`. **Next: nothing in slice 12 — it's feature-complete; the live-key LLM run + the 12d/12e browser sign-off are the only manual steps left (both non-blocking, owner-run).**
- **2026-07-15** — **Slice 12d built (UI: resume upload + fit surfacing) — frontend-only, zero backend change (12c already shipped the DTO/contract).** Decisions during the build: (1) **Resume management lives in Settings, not a new nav view** (`settings/ResumePanel.tsx`, colocated `.test.tsx`) — least churn, matches SPEC §11 "Settings or a small Resume view", no `App` `View`/route change. It runs its own `['resumes']` query (independent of the Telegram query, so it renders even if Telegram is loading/erroring); **add / activate / delete each invalidate both `['resumes']` and `['jobs']`** so an active-resume change re-scores the Jobs list. (2) **The active resume is the server-owned singleton** (`GET /resumes` → the row with `active:true`); `JobsPane` reads it via its **own** `['resumes']` query and threads `resumeId` into the jobs query key + `?resume=`. So "when a resume is active" = the server flag, not a URL/React state — one source of truth, shared across views via the React Query cache. **When no resume is active the behavior is byte-identical to before 12d**: no `resume=` param, no fit badge, Fit sort hidden (a dedicated test asserts every `/jobs` URL omits `resume=` and no `Fit` control renders). (3) **`SortBy` gained `'match'`; `fetchJobs` only emits `sort=match` when `resume !== null`** (fit-sort is meaningless without a resume, and the Fit segment is hidden without one, so the URL can't lie); `JobsPane` additionally **normalizes a requested `sort=match` back to `tier` while `resumeId` is null** (the resumes query's warm-up, or a hand-crafted URL) so the control/label never shows a fit sort that isn't in effect. Default sort still omits the param (`tier`) for clean shared URLs. (4) **Fit renders as a soft signal exactly like sponsorship** (SPEC §11): a quiet teal "Fit N" badge in the row meta (`JobList`, `--accent-soft-*` tokens), and a drawer **Fit card** (`JobDrawer` `FitCard`) with the overall score, three sub-score bars (Skills / Level / Sponsor-country — category alignment folds into `overall`, no bar of its own, matching the stored columns), matched (green) + missing (grey) skill chips, and a "soft signal, never hides a posting" note. **DESIGN.md predates the resume feature**, so — per CLAUDE.md "DESIGN.md is the frontend-design authority until the design skills exist" — the card is styled consistently with the existing sponsorship card using only existing tokens (no invented colors). (5) **The drawer Fit card takes its score as a PROP from the already-scored list row** (`data.jobs.find(id).match_score`), *not* a re-fetch — the row is page-bounded-scored (§11), and `GET /jobs/{id}` is deliberately left unscored (no `?resume=` on the detail endpoint), so the Fit card needs zero extra network. `JobDrawer` gained a required `matchScore: MatchScore | null` prop. (6) **`.txt` upload is read client-side via `FileReader` into the paste textarea** (SPEC's always-available path; fills the label from the filename if blank); **PDF still deferred** (paste/`.txt`-only, the 12b decision stands). (7) **Test-harness update (behavior changed, so the boundary mocks changed):** `JobsPane` now fetches `/resumes`, so the `JobsPane`/`CountriesPage`/`App` test fetch-mocks gained a `/resumes` endpoint (→ `[]` where no resume is set), and the brittle `fetchMock.mock.calls[0][0]` assertions in `JobsPane.test` were replaced with a `jobListUrls()` helper that filters to `/jobs` list requests — robust to the extra `/resumes` fetch regardless of ordering. **Net +13 frontend tests (46→59); `make verify` + `vite build` green. Next: 12e (LLM deep-match, key-gated off).**
- **2026-07-15** — **Slice 12c built (Tier-1 heuristic scoring wired into `/jobs`; cached, page-bounded).** Decisions during the build: (1) **New application module `application/scoring.py`** (kept distinct from `domain/matching.py`, which is company-name matching — different layer, different concern). Holds `score_jobs_for_resume` (the cache-gated use case), `list_scored_jobs` (the read-side orchestrator), and `job_facts_from_listing`. Imports domain only — no adapter reach (golden rule 1). (2) **`MatchScoreRepo` port + `SqliteMatchScoreRepo`** (the interface deferred from 12b, now pinned by the cache tests). Cache lives in `job_match_scores`, PK `(resume_hash, job_canonical_id)`, storing `content_hash` alongside so a re-poll of an **unchanged** posting reuses its score and a **changed** posting (new `content_hash`) recomputes only itself — `content_hash`-gated exactly like classification. `matched/missing_skills` stored as sorted JSON arrays. Repo is dumb; the reuse/recompute policy lives in the use case. `CachedScore` (score + content_hash) is the read model the gate reads. (3) **`JobFacts` is assembled in the application layer from the read-model `JobListing` row + the job's description** (per the SPEC §11 note "assembled by the application layer from a canonical job"). The listing already carries categories/level/country/sponsor_tier (converted string→enum here); the only extra columns needed are `content_hash` (cache gate) and `description` (skill source), fetched by a new **`JobRepo.get_scoring_inputs(job_ids)`** — a targeted **by-id** fetch for the page's ids, *not* a second listing query (honors the refactor-watch "don't fork the jobs query"; the public read model stays free of `description`). (4) **Scoring is bounded to the returned window** — `get_scoring_inputs`/`score_match` run for page-size ids only, never the whole table (`test_jobs_scores_current_page_only` asserts the cache grows by page size, not row count). The cache accumulates across page visits, so it still "covers" the whole DB over time at $0 (Tier 1 is pure/deterministic). (5) **`sort=match` uses two cooperating layers:** `search()` gains an optional `LEFT JOIN job_match_scores` (keyed on `filters.resume_hash`, new `JobFilters` field) that **orders by the *cached* fit score** so the correct window is drawn cross-page from a warm cache (uncached rows sort last via `ms.overall IS NULL`, tie-broken by the normal sort_rank/posted_at); then `list_scored_jobs` **re-sorts the returned window by the *fresh* score**, so the page is exact even on a cold or stale cache. Known trade-off (documented, acceptable for a single-user tool): the very first `sort=match` on a >1-page dataset with a cold cache selects page 1 by the default order before re-sorting it by fit — global ordering sharpens as the cache warms (or set `limit=200` to score everything at once). A dedicated test (`test_jobs_sort_match_ranks_across_pages_from_warm_cache`) exercises the SQL-join path: a null-`posted_at` role that the default sort buries last is surfaced to page one once the cache is warm. (6) **Default sort is untouched and fit never filters** — `/jobs` with no `resume` param behaves exactly as before (`match_score` is `null`), and `?resume=` with the default sort attaches scores but leaves ordering unchanged (verified: order identical to the no-resume page). An **unknown `resume` id → 404** (a stale reference surfaces rather than silently returning unscored rows). (7) **DTO/type:** `JobListing.match_score` (optional, defaulted `None`) → `JobOut.match_score: MatchScoreOut | None` → `types.ts` `MatchScore` + optional `Job.match_score` (the "one place" for the shape; the UI that renders it is 12d). Adding `get_scoring_inputs` to the `JobRepo` protocol required stubbing it in the two in-memory `FakeJobRepo`s (ingest, saved-search matching — neither scores). **Next: 12d (UI).**
- **2026-07-15** — **Slice 12a built (pure resume-fit domain) + an enabling layer refactor.** (1) **Keyword vocabulary moved `adapters/classify/keywords.py` → `domain/vocabulary.py`** (commit `7cf043c`, behavior-preserving, 475 tests unchanged). **This deviates from SPEC §11's literal "`build_profile` reads `adapters/classify/keywords.py` from the domain" and from the old CLAUDE.md keyword-location line** — both now updated. Rationale: a pure domain `build_profile` reading an adapter module is exactly the domain→adapter import golden rule 1 forbids (the top-priority refactor trigger, which the PLAN 12a "refactor watch" anticipated). Once a second domain-facing consumer appeared (the matcher), the vocabulary + word-boundary matching became shared domain knowledge, not an adapter's private data; `HeuristicClassifier` is now a thin adapter that keeps only its policy (title-only categories, years from body) and delegates matching. (2) **`resolve_level(level_text, years_text)` extracted** as the single level rule shared by the classifier and `build_profile` (kills the level-from-years duplication before a 2nd copy landed); callers differ only in text scope. (3) **`score_match` is a pure weighted blend**, sub-scores in named constants: skills 0.40 / category 0.25 / level 0.20 / sponsor 0.15. **Skill fit = coverage of the job's skills by the resume (|A∩B|/|B|), NOT raw Jaccard** — Jaccard penalises a broad resume against a focused job's skill list, inverting fit quality; coverage also gives the exact matched/missing breakdown the drawer shows. (4) **Level fit: over-qualification penalised harder (0.30/step) than a stretch up (0.12/step)** — a senior taking a junior role fits worse than reaching toward staff; UNSPECIFIED either side → neutral 0.70, never a wall. (5) **Sponsor/country fit** = tier base (yes 1.0 / registry 0.75 / unknown 0.40 / no 0.0) × 0.5 when the job's country is outside the resume's `target_countries` (empty target = country-agnostic). It's a soft *nudge* (0.15 weight), never a filter — country filtering stays a separate axis (verified in a sanity spot-check: iOS/NL/registry 96 > same job in US/unknown 88 > frontend/US/no 20). (6) **`JobFacts` is a new scoring-input value object** (skills/categories/level/country/tier) kept distinct from `NormalizedJob` so scoring stays pure and independent of ingest/persistence shapes; the application layer (12c) will assemble it from a canonical job. (7) **`MatchScore` mirrors the `job_match_scores` columns** (overall + skills/level/sponsor sub-scores + matched/missing) — **category alignment folds into `overall` with no stored column of its own** (SPEC §7 lists only those three sub-scores). **(8) Then built 12b (resume ingest) in the same session.** Migration 008 creates both §11 tables (`resumes`, `job_match_scores`) up front (additive/forward-only); `ResumeParser`/`ResumeRepo` ports; zero-dep `PlainTextResumeParser`; `SqliteResumeRepo` (dumb, owns only the active-singleton `UPDATE ... CASE WHEN id=? THEN 1 ELSE 0`); `ingest_resume` use case owning the dedup (identical `resume_hash` reuses the row, not re-profiled) + active-singleton policy; `/resumes` CRUD API (POST paste-text JSON, GET, PUT `/active`, DELETE). **PDF-dep decision: I chose PASTE/`.txt`-ONLY (zero-dep) autonomously** (the question timed out while you were away) — it's the reversible, minimal default matching SPEC's "always-available path" + "every dependency must earn its install"; `PdfResumeParser` is a documented one-commit drop-in behind the port, and the API upload is JSON text (frontend reads `.txt` client-side). **`MatchScoreRepo` deferred to 12c** (its interface is pinned by the cache-behavior tests there, not guessed now — the table exists so nothing blocks it). **Next: 12c.**
- **2026-07-14** — **Slice 12 (resume-match fit scoring) added to SPEC §11 + PLAN slice 12; not built yet (commit `f499528`, docs only).** Feature: upload a resume once, score how well each posting fits it, sort by fit, and on-demand get an LLM rationale for a single job. **Design = the repo's established two-tier heuristic→LLM pattern (slice 9), chosen specifically to make multi-job scoring feasible:** Tier 1 = a **pure domain `score_match(profile, job)`** (skill overlap + category + level + sponsorship/country fit), free/deterministic, cached by `(resume_hash, content_hash)` and **bounded to the current `/jobs` page** — so it scales to the whole DB at $0. Tier 2 = an `LLMMatcher` behind a `Matcher` port, **one call per job on demand** (drawer "Assess fit") or a bounded top-N, reusing the **existing slice-9 `LLMBudget`** (no second budget) — never scores the whole DB (that'd be O(jobs)×cost, explicitly out of scope). Resume profile reuses the classifier keyword tables (one skill vocabulary; resume category/level computed the same way a job's are → directly comparable). **Fit is a soft signal / opt-in `sort=match` key, never a default filter-out** (same rule as sponsorship; default sort unchanged). **Zero changes to the ingest/classify pipeline** — matching is a read-side concern layered on canonical jobs (golden rule 1). New tables `resumes` + `job_match_scores`; new ports `ResumeParser`/`Matcher` (+ `ResumeRepo`/`MatchScoreRepo`). Build order 12a domain → 12b resume ingest → 12c heuristic on `/jobs` → 12d UI → 12e LLM (last, **key-gated off by default** like slice 9). **Two conventional defaults flagged for user confirmation before 12b:** (1) PDF parsing adds one scoped dep (paste/`.txt` is zero-dep and always available; DOCX deferred) — could stay paste-only; (2) Tier-2 LLM parked until `BEACON_ANTHROPIC_API_KEY` is set. **Next action to start: PLAN 12a — red tests for `build_profile`/`score_match`.**
- **2026-07-13 (Beacon-2)** — **Second UI handoff (`~/Downloads/Beacon-2.zip` → `design_handoff_beacon/Beacon.dc.html`) applied: Countries home goes stacked → two-pane.** The `.dc.html` prototype is the authority; its bundled README/SPEC prose is internally inconsistent (describes a **236px light** sidebar while the actual prototype ships the **88px dark** rail we already have), so I treated the runnable prototype file as ground truth and kept the existing dark 88px rail + dark drawer (both already matched). **Real deltas shipped:** (1) `CountriesPage` `.main` is now a fixed-height (`100vh`, `overflow:hidden`) flex column; below the header a **two-pane `.row`** holds the globe fixed left (`flex:1.35; min-height:420px`) and a dark scrolling **`.sidePanel`** right (`flex:1; min-width:372; max-width:512; overflow-y:auto`, `#071a22`). The side panel shows the all-markets card stack (no selection) **or** the jobs pane (selection) — beside the globe, replacing the old below-the-globe stack. (2) **`JobTable` (wide grid) → `JobList` (compact stacked cards)**: title/company + star/hide, then a sponsor-chip · city · level · posted meta row. Per-row "open original" link and category chips were **dropped from the card** (the link lives in the drawer; categories remain filter pills) — faithful to the prototype's job card. (3) `JobsPane` now renders inside the side panel with a **sticky header** (back "All markets" — was "All countries" — + "Jobs · {Country}" + count) and a new **relocation-reference legend** (Work visa / PR path / Citizenship + verified date for the selected market, passed down as a `country` prop from `CountriesPage`); its filter bar is no longer sticky/gradient and wraps within the narrow panel. (4) Country cards are compact/dark (2 blocks — Work visa + PR path; **Citizenship moved to the legend**), name 15px, in a vertical stack with an "N markets · tap a beacon or a card" caption. **Tests updated to match the design-driven behavior:** dropped the per-row-link test (now drawer-only), the category-chips-per-row test became a city+level meta test (`data-testid` `job-table`→`job-list`), the country-card test no longer asserts citizenship (asserts it in the legend after selection), and "All countries"→"All markets". `verify-frontend` green (eslint + tsc + **46** vitest) + `vite build` clean. **Not committed** (left for the user) and **`DESIGN.md` not yet rewritten** to the two-pane spec — both flagged in Next action. Sidebar Settings gear kept off-prototype (functional Telegram config).
- **2026-07-13** — **UI redesign kicked off from a new design handoff (`~/Downloads/Beacon.zip` → `Beacon.dc.html`, 2026-07-13). The handoff supersedes the light "Nordic Slate & Teal" prose in DESIGN.md/README.** Ground truth = the `.dc.html` prototype + the user's two screenshots (confirmed): the app is a **dark holographic theme** (`#04121a` bg, teal `#5eead4`), **Countries is the home page** with a **procedural 3D canvas dot-globe** (Jakarta→country beacon arc, auto-focus, drag-rotate, tap-pin/tap-ocean), source-health folded into a **floating glass widget on the globe**, an **88px icon-rail sidebar** (Globe + Saved only), and the **Jobs list rendered as a pane inside Countries** (gated by a selected country) rather than a standalone route — Companies removed from nav, Settings dropped from nav. **Shipped this session (commit `redesign:` on branch `feat/dark-globe-redesign`):** the theme flip (`tokens.css`/`index.css` full dark palette, verbatim hex), the globe (`countries/globeGeo.ts` LAND/SEA/PIN_GEO + projection math ported verbatim; `countries/Globe.tsx` canvas rAF engine, jsdom-guarded, sr-only pin buttons for a11y/tests), and the dark Countries home (globe panel, overlay header/legend, caption, source-health widget, dark cards). Also fixed a **pre-existing dev bug**: `vite.config` only proxied `/jobs`+`/healthz`, so Countries/Companies/Searches/Settings pages silently failed in dev (SPA fallback → JSON parse error); added the missing proxies. `make verify` green (475 backend + 50 frontend). Visually verified against the screenshot (headless Chrome). **User approved all recommended defaults (full dark, drop Companies + keep Settings off-nav, port globe verbatim); redesign completed in a follow-up commit:** 88px icon-rail (Globe/Saved nav + Settings gear + vertical "07:04·LIVE"; Companies dropped from nav); `JobsPage`→embeddable `JobsPane` rendered inside `CountriesPage`, gated by `?focus=CODE` (selecting a country seeds `country=CODE&status=all&focus=CODE`; back button/ocean-tap/Globe-nav clear it); dark re-theme of filter bar/segmented controls (teal-tint active)/dropdowns (P/☆ country badges)/drawer CTA; `COUNTRY_OPTIONS`+`countryName()` moved to `jobs/taxonomy.ts`; Companies page deleted; DESIGN.md rewritten to the dark spec. `make verify` green (475+47). Verified all surfaces (home, jobs pane w/ beacon arc, drawer, saved searches) in a real browser. **Source-health widget is still static** (44/1/2, poll 07:04, per the design's "static summary widget") — TODO wire live counts from `/companies` health rollup (`api/companies.ts` kept for this).
- **2026-07-11** — **Slice 9 LLM classifier deliberately parked (disabled) for now — key-gating is the off-switch, no code change.** Confirmed the LLM is already fully inert: `make_classifier` (`adapters/classify/factory.py`) returns a bare `HeuristicClassifier` whenever `BEACON_ANTHROPIC_API_KEY` is unset, which it is (no `.env`, not in shell). Both composition roots (`ingest.py`, `classify.py`) and the scheduler (via `run_ingest`) route through that one factory, so there is **no path to Anthropic** and zero spend possible today. Chose to leave the code as-is rather than add a `BEACON_LLM_ENABLED` hard flag — for a single-user tool the key *is* the switch; re-enable by setting the key, nothing to un-flag. **Impact of heuristic-only:** only the classification residue is affected — ambiguous-title roles (Research Engineer, Data Scientist, generic SWE, security/support/solutions eng) keep `categories=''`, so no category chip and excluded from `category[]` filters (still listed; level largely unaffected — heuristic reads seniority tokens + years regex directly). Everything else (sponsorship tiers, dedup, user status, digest matching, source health, countries) is independent of the LLM. `scripts/spot_check_llm.py` and `beacon.classify --upgrade-residue` remain safe no-ops (run, return heuristic results, improve 0 rows). Fully reversible.
- **2026-07-09** — **Slice 11 built (source health & recovery); decisions during the build (9 green sub-slices):** (1) **`SourceHealth` is a pure domain state machine** (`domain/health.py`): `FailureKind` (gone/unreachable/schema_drift) + `Health` (ok/degraded/quarantined) + `record_failure`/`record_success`/`should_poll`, thresholds as named constants (gone & schema_drift quarantine after **3**, unreachable after **10** — a moved slug or shape change needs a human fast; a 5xx streak is given patience). `consecutive_failures` counts any consecutive failures and the applied threshold is the latest failure's kind; `reason` holds the current-streak kind (populated while degraded *or* quarantined, so the digest/UI can show "2 failures · 5xx"), cleared on success. (2) **No new migration** — the four health columns (`consecutive_failures`, `last_success_at`, `health`, `quarantine_reason`) already exist since migration 001 (created ahead for this slice); the schema test already asserted them. `quarantine_reason` is reused to store the current-streak kind (its name is slightly narrow but it's the one free-text health-reason column SPEC §7 lists). (3) **The httpx→FailureKind mapping lives at the HTTP door, not in the application:** `PoliteClient` catches httpx errors and raises `SourceUnavailable(kind)` (404/410→gone, other HTTP status + transport/timeout→unreachable) — a new `application/errors.py` exception carrying a domain `FailureKind`, so `ingest_source` records health **without importing httpx** (Clean-Arch: application stays IO-free). (4) **`ingest_source` returns a failure, never raises for a source failure:** `SourceUnavailable`→its kind; any other fetch exception (JSON shape wrong before the posting loop) → schema_drift; **a non-empty response where every posting fails to normalize** (fetched>0, upserted==0) → schema_drift. Critically, a failed poll (including that all-normalize-fail case, whose `seen` set is empty) **never runs the closed-sweep** — this is the same invariant slice-10 established (`test_failed_poll_never_closes_jobs`), now generalised so a schema-drifted board can't mass-close its jobs either. An **empty board** (fetched==0) is a *successful* poll and still sweeps. (5) **`ingest_all` gained a `company_repo` param** (reads health, filters `should_poll`, records success/failure per company); a quarantined company is skipped entirely → no fetch, no sweep, **jobs frozen** by construction (no separate "freeze" code needed). Failed polls now appear in the `results` dict too (with `.failure` set) — a small behavior change so the CLI/caller sees every attempted poll. (6) **Reslug recovery is a pure CSV edit, made automatic:** `SqliteCompanyRepo.upsert` resets health/counters/`last_success_at` **only when `ats_type` or `ats_slug` actually changed** (a SQL `CASE` in the ON CONFLICT, like the slice-5.5 user_status reset-on-hash-change). So fixing a moved board = edit `seeds/companies.csv`, next run re-seeds → quarantine cleared → polls normally; an unchanged re-seed never un-quarantines (every run re-seeds). SPEC said "reset health" as a separate step; folding it into the slug-change makes recovery genuinely no-code. (7) **Weekly restore probe** (`probe_quarantined`) retries each quarantined company once via a full `ingest_source`; success → `record_success` (restore + resume upserts), failure → **left untouched (no counter inflation)**. Scheduled Monday 05:00 LOCAL_TZ; `run_probe` is its composition root. (8) **Health in the digest:** `Digest` gained a source-health section (`HealthAlert` = quarantined company+reason+last-ok-date, `RegistryStale` = snapshot>45d), rendered as a leading "⚠ Source health" block; either makes an otherwise-empty digest **send** (silent decay is the whole point). `build_health_alerts` translates `CompanyRepo.list_health()` + `stale_registries(meta)` into those; wired into `match_saved_searches` (new kw params) via `run_ingest`. **Known trade-off:** health alerts have no seen-dedup, so they re-fire every poll cycle (4–6h) while quarantined — acceptable for a single-user tool (fix the slug and they stop); a dedicated daily-only health digest is a future option. (9) **`registries_meta` now written** by `refresh_registries` (new optional `meta_repo`+`now`, recorded in the refresh CLI) — the slice-2-deferred bookkeeping; `RegistryMeta` + `stale_registries` (45-day, `REGISTRY_STALE_AFTER_DAYS`) are pure domain; registries never quarantine, they just nag. (10) **`GET /companies/health`** + `get_company_health` query computes the DESIGN §3 view: per-company status where a seed with an unsupported ATS (no adapter) shows **`pending`** (never polled → its default-ok health isn't meaningful), plus summary counts (seed/supported/healthy/degraded/quarantined/pending + per-ATS). Shadow employers (`ats_type='none'`) are excluded — they aren't pollable sources. `SHADOW_ATS_TYPE` moved to `domain/company.py` (it's a company fact) so the repo can reference it without importing a use-case module. (11) **Frontend Companies view** (DESIGN §3): summary cards, a computed mono seed line (supported ATS enumerated + N awaiting adapters — never hardcoded), and a health table (Company · ATS·slug · HQ · Last success · Health badge+reason); source-health color tokens added to `tokens.css` verbatim; 5th nav item. (12) **UI defers (recorded):** per-job "source stale since <date>" banner on the Jobs view + the sidebar source-health footer legend + nav count badges — the Companies view + `/companies/health` + digest cover the acceptance; the Jobs banner needs `JobListing` to carry company health (extra plumbing), deferred consistent with prior UI-defer precedent. (13) **Acceptance is live** (`scripts/spot_check_health.py`, the slice-11 gate): real Greenhouse 404 → gone → quarantined after 3, job never closes, digest reports it; fix slug → health reset → recovery poll upserted 15 Tines jobs. Ran green.
- **2026-07-09** — **Slice 10 built (CountryPanel + RemoteOK/WWR + scheduler); decisions during the build (7 sub-parts, committed in green steps):** (1) **Country visa reference is pure domain data (`domain/visa.py` `COUNTRY_REFERENCE`, 11 rows + `PriorityTier`), the source of truth; the `countries` table (migration 006) is a seeded projection** re-seeded at every API-lifespan + ingest-CLI startup (idempotent upsert keyed on `code`), so the table can never drift from the constant. Editing a figure = editing the constant + bumping its `verified_at`. All rows `verified_at=2026-01-15` (SPEC §4's "as-known Jan 2026"); `source_url`s are the official immigration-authority roots; summaries lifted verbatim from SPEC §4, `registry_name` mirrors SPEC's "Registry data source" column (a note, `NOT NULL` — Sweden's says *why* none exists). Priority tiers straight from SPEC §3 (7 primary / 4 nice-to-have). A `CountryRepo` port keeps it behind the Clean-Arch boundary even though it's static reference data. (2) **The slice-2-deferred "which registers matched + confidence" now lands on `JobDetail`** — added `registries: tuple[str,...]` + `match_confidence` to the read model, decoded from `companies.registry_flags` by a new pure `domain.registry.registry_names(flags)` (UK|NL|US|MANUAL in bit order); the drawer's registry_inferred card lists them. No new query — the existing detail join already had the company. (3) **Job-detail drawer (DESIGN §2) opens via a URL `?job=<id>` param** (shareable, Back-button closes — same rationale as the filter/view params), not React state. Opening a `new` job fires a `seen` PATCH (event-driven in the open handler, not an effect); the status mutation invalidates both `['jobs']` and `['job']` so list and drawer stay in sync. The row's role cell became the open-trigger `<button>` (accessible; the star/hide/link stay separate buttons, no nested-interactive HTML). `TIER_LABEL` extracted to `taxonomy.ts` (now shared by table + drawer). Status-pill / drawer / country-panel colors added to `tokens.css` verbatim. (4) **RemoteOK + WWR are company-less sources** (like HN/JobTech) — each posting names its employer, so they set `NormalizedJob.company_name` and go through `ingest_companyless_source`. RemoteOK drops the leading legal-notice array element (no `id`). **WWR is RSS/XML → the `Fetcher` port gained `get_text`** (raw body); `PoliteClient` refactored to one `_get(url, params, parse)` path serving both `get_json`/`get_text`. WWR `fetch()` parses `<item>`s to dicts (stdlib ElementTree — a trusted feed, not hostile HTML), `normalize()` splits the "Company: Role" title (no colon → `company_name=None`, skipped by the company-less ingest's per-item guard); RFC-822 pubDate via `email.utils.parsedate_to_datetime`. (5) **Closed-posting sweep needs a per-job miss counter → migration 007 `jobs.consecutive_misses`** (not in SPEC §7's jobs column list; added like slice-2's `match_evidence`). `JobRepo.sweep_absent_jobs(source_id, company_id, seen, now, threshold)` runs **only at the end of a successful poll** (after `ingest_source`/`ingest_companyless_source`'s fetch+loop) — a failed fetch raises before it, so absence can only accrue from successful polls (SPEC §7; `test_failed_poll_never_closes_jobs` = 5 failing cycles → never swept). Scope is `(source_id, company_id)` for ATS (many companies share an ATS `source_id`) and `source_id`-only (`company_id=None`) for company-less sources. Seen jobs reset to 0 misses **and reopen** (`closed_at=NULL`); absent jobs increment and get `closed_at` at `CLOSE_AFTER_MISSES=2` (domain constant). (6) **Scheduler = APScheduler `AsyncIOScheduler`, wiring only** (`beacon/scheduler/schedule.build_scheduler`): `poll_ats` (4h), `poll_boards` (6h), `refresh_registries` (monthly cron), `nightly_backup` (nightly cron), all keyed in `LOCAL_TZ`. `beacon.ingest._run` promoted to public `run_ingest` with `poll_ats`/`poll_boards` toggles so the two source families run on their own intervals (dedup+notify always run — idempotent, so either poll keeps the list consistent); `refresh._run_refresh`→`run_refresh`. **HN's daily-first-week cadence is folded into the 6h boards poll** (its per-thread unseen-kids cache makes frequent re-polls cheap, slice 7) — a recorded simplification, not a separate trigger. `apscheduler` dep added (v3.11, no type stubs → a scoped `[[tool.mypy.overrides]]` ignore). launchd plist fixed (Downloads→Desktop path, enabled, RunAtLoad+KeepAlive, `/tmp` logs). (7) **Nightly backup uses sqlite3's online `backup()` API** (`adapters/persistence/backup.py`) → timestamped `beacon-YYYYMMDD-HHMMSS.db` under `Settings.backups_dir` (new env `BEACON_BACKUPS_PATH`, `backups/` gitignored), pruned to newest 14; `scripts/backup_db.py` manual entry, scheduler runs it nightly. (8) **Acceptance:** the Swedish-card and 404-no-close boxes are proven (drawer test + live `/countries`; `test_failed_poll_never_closes_jobs`); **reboot-resume is the one manual step** (launchd load + reboot on your box) — scheduler build+start is verified (4 jobs scheduled with correct Asia/Jakarta next-run times).
- **2026-07-09** — **Slice 9 built (LLM fallback classifier); decisions during the build:** (1) **The LLM is a transparent upgrader behind the one `Classifier` port, not a pipeline branch.** `TieredClassifier(heuristic, llm, budget)` *is* a `Classifier`, so `ingest_source`/`ingest_all`/`ingest_companyless_source`/`backfill` are untouched — they still take a single `classifier` and never learn the LLM exists (golden rule: new capability, zero changes to application/domain). Wired in only at the composition roots via `make_classifier` (mirrors `make_notifier`): heuristic-only until `BEACON_ANTHROPIC_API_KEY` is set, else the tiered classifier. (2) **The ambiguity gate is a single pure predicate `Classification.is_ambiguous` (= empty category set)** in the domain (mirrors `TelegramConfig.is_configured`), so "ambiguous" is defined in exactly one place ("explicit ambiguity signal, not a magic number sprinkled around"). **Level-UNSPECIFIED alone is NOT ambiguous** — `unspecified` is honest (SPEC §6) and not worth a paid call once the category is known; only empty *categories* trigger the LLM. (3) **The LLM is a strict upgrader, never a dependency:** it's consulted only when `is_ambiguous` AND the budget allows, and **any** failure (bad JSON, HTTP/transport error, refusal) is caught and degrades silently to the heuristic result (CLAUDE.md LLM rule). When it fills the empty categories it must **not erase a level the heuristic was sure of** (a title token like "Senior") — so an LLM `unspecified` level falls back to the heuristic's level. (4) **`Classifier` port stays synchronous; `LLMClassifier` uses a sync `httpx.Client`** — deliberately no async ripple across the heuristic/backfill/tests. Justified because the LLM is a paying-customer API (not a polite crawl → does NOT route through `PoliteClient`/`Fetcher`) and classification runs inside the **sequential** per-posting ingest loop, so a blocking classify stalls nothing that could otherwise progress. (5) **Raw httpx to the Anthropic Messages API, no `anthropic` SDK dep** (matches the repo's thin-httpx style, e.g. `TelegramNotifier`, and keeps it fully `MockTransport`-testable): `POST /v1/messages` with `x-api-key`+`anthropic-version`, **JSON-only prompt** enumerating the Category/Level values, `max_tokens=256`, description clipped to 2000 chars for token cost. Parser is **tolerant** (strips prose / ```json fences via first-`{`…last-`}`; drops unknown category values; junk level → UNSPECIFIED) but **raises `ValueError` on truly unparseable replies** so the tiered fallback fires. Recorded response-shape fixtures under `tests/fixtures/anthropic/`. (6) **Hard monthly call cap = new `LLMBudget` port + `SqliteLLMBudget`** over `llm_usage(month, call_count)` (migration 005, "registries_meta-style" bookkeeping). `try_reserve()` **counts the attempt before the call** (conservative — a failing/retrying call can't blow the budget). Month keyed in a **new shared `LOCAL_TZ` constant (Asia/Jakarta)** in `config.py` — the budget resets on the local month boundary (SPEC §9); this introduces the one `LOCAL_TZ` the slice-10 digest day-boundary also needs. Clock injected for deterministic tests. Default cap `BEACON_LLM_MONTHLY_BUDGET=500`; model `BEACON_LLM_MODEL` default `claude-haiku-4-5-20251001`; key as `SecretStr` (kept out of reprs, like the Telegram token). (7) **Two distinct backfills, preserving slice 3's contract:** `backfill_classifications` still handles `categories IS NULL` (never-classified) and — with the tiered classifier wired — LLMs their residue; the new `upgrade_ambiguous_classifications` (+ `JobRepo.list_ambiguous`) is the **catch-up for the pre-LLM backlog** of `categories=''` rows (heuristic-classified empty), rewriting only rows it actually improves. Kept separate + opt-in (`python -m beacon.classify --upgrade-residue`) so slice 3's "'' = classified, nothing matched → skipped" rule holds for the plain backfill and the LLM spend stays explicit. Re-running `--upgrade-residue` re-attempts still-empty rows (bounded by the monthly budget) — acceptable/documented. (8) **Backend-only slice** — category/level already surface on `/jobs` (slice 3), so the LLM just fills more of them; no API/frontend change. Both CLIs now print `llm_calls_this_month`. (9) **Acceptance is inherently a live run** (does the LLM actually improve the residue?) — the mechanism is proven in the suite with fixtures/`FakeLLMClassifier`, but the PLAN acceptance box needs `scripts/spot_check_llm.py` run with a real key (pending, like slice-8's live send).
- **2026-07-09** — **Settings UI for Telegram creds (slice-8 extension, closes the live-send acceptance from the browser); decisions during the build:** (1) **Creds are user-editable runtime config, stored in SQLite behind a new `SettingsRepo` port** — deliberately distinct from the env-read `Settings` dataclass (deployment config: db_path, registry paths), which stays env-only. New migration `004_app_settings.sql` = a generic `app_settings(key TEXT PK, value TEXT NOT NULL)` k/v table (extensible; today only `telegram_bot_token`/`telegram_chat_id`); `SqliteSettingsRepo.set_telegram_config` persists verbatim (a None field DELETEs its key), keeping the repo dumb. (2) **`TelegramConfig` is a pure domain value object** (`domain/notification.py`, `frozen/slots`), NOT pydantic — `bot_token` is a plain `str | None` with `field(repr=False)` so it never leaks into a repr/log, keeping domain framework-free (the config-layer `Settings` keeps its `SecretStr` and unwraps once in a new `Settings.telegram_config()`). It owns `is_configured` and `merge(fallback)` (per-field: this wins, fallback fills blanks). (3) **Precedence: DB-set creds win per-field over `BEACON_TELEGRAM_*` env** (`effective_telegram_config = repo.get().merge(env)`), resolved identically by the ingest CLI and the API — so the UI is authoritative but env still works as a bootstrap/fallback and the spot-check script is unaffected. (4) **Notifier selection extracted to one shared factory** `adapters/notify/factory.make_notifier(config, client)` (was a private fn in `beacon.ingest`); both composition roots (CLI + `/settings/telegram/test`) call it → Telegram when both creds present, else Stdout. (5) **Token is write-only over the wire** — `GET /settings/telegram` returns `{chat_id, bot_token_set: bool}` and never the token; `PUT` body `{chat_id, bot_token}` with 3-state token semantics (`null`→keep stored so a chat_id-only save doesn't wipe the secret; `""`→clear; value→set) — the keep/clear policy lives in the `update_telegram_config` use case, not the repo. (6) **`POST /settings/telegram/test` fires a real one-line digest** through the effective notifier and returns `{ok, channel}` (`telegram`|`stdout`); a rejected send **or** a network failure is caught as `httpx.HTTPError` (not just `HTTPStatusError`) and surfaced as a **400 with Telegram's own reason** (e.g. "chat not found"/"Unauthorized"), never a 500. This converts the slice-8 "fire a real message" acceptance from a terminal script into a UI button. (7) **The outbound HTTP client is a `get_http_client` FastAPI dependency** (async-generator, 15s timeout) so the async test route never blocks and tests override it with a `MockTransport` client (GET/PUT stay sync `def` — sqlite only — resolving `EffectiveConfigDep` in the threadpool). (8) **Frontend: Settings is a third nav view** (`?view=settings`, `App` `View` type extended + `parseView` guard); `SettingsPage` uses an **uncontrolled form keyed on the loaded settings** (avoids the useState-mirrors-server-state antipattern; the key remount clears the password field after a save+refetch). Live-verified end-to-end against a real server: GET/PUT masking + token persistence across a chat_id-only save, stdout fallback prints the digest, bogus creds hit real `api.telegram.org` → 401 → clean 400.
- **2026-07-08** — **Slice 8 built (saved searches + Telegram digest); decisions during the build:** (1) **`SearchFilters` is the matchable subset of the Jobs filter bar** (q, countries, categories, levels, tiers) — NO sort/status/pagination (those are view concerns). It's a pure domain value object (`domain/saved_search.py`) with `filters_to_json`/`filters_from_json`; the round-trip (lists→tuples so equality holds) is the contract (`test_filters_json_roundtrip`). Stored in `saved_searches.filters_json`. (2) **Tier filter is membership, not a threshold.** The acceptance phrasing "tier≥registry_inferred" is captured as `tiers=(registry_inferred, explicit_yes)` — a membership set matching the existing FilterBar chips / `/jobs` `sponsor_tier[]` param, so "save this search" just snapshots the current chips (no new threshold concept). (3) **`match_reason` is a pure domain fn** naming which of the search's criteria a job satisfied, fixed order category·country·level·tier, only constrained dimensions shown, job's own matched values (e.g. "ios · SE · senior · registry_inferred"); unconstrained → "all". Stored per row in `seen_matches.match_reason`. (4) **Digest is channel-agnostic** (`domain/digest.py`: `Digest`/`DigestGroup`/`DigestLine`) and the message *split* is a pure `build_messages(digest, *, max_chars)` — the 4096 limit is Telegram's, owned by `TelegramNotifier` (`TELEGRAM_MAX_CHARS`), passed in, so the packer is unit-testable with any size. Packing keeps a group header with its first entry (never orphaned); a group spanning messages doesn't repeat its header (acceptable for a personal tool); only a single oversize entry is hard-split. (5) **`Notifier.send(digest)` takes the structured digest, not pre-chunked text** — each notifier owns its own limit/formatting (Telegram chunks at 4096; Stdout at 100k). Plain text, **no `parse_mode`** (job titles carry markdown-ish chars that would break rendering). (6) **`match_saved_searches` sends BEFORE recording** `seen_matches` — a notifier failure leaves matches un-recorded so they retry next run (proven by `test_send_failure_prevents_recording`); `record_matches` is `INSERT OR IGNORE` (idempotent, first notification wins). `last_run_at` is touched for every search that ran, matches or not. (7) **`seen_matches` PK is (search_id, job_canonical_id)**, FK to `jobs(id)` kept (a match always references a real canonical job; jobs are never deleted) and `ON DELETE CASCADE` from `saved_searches` (delete a search → its ledger goes too). (8) **`/searches` CRUD = POST/GET/DELETE** (no PATCH — edit = delete+recreate for a personal tool). The card's **"N new" count = matching canonical jobs with `user_status='new'`** (mirrors the nav new-count semantics), computed via the shared `to_job_filters(..., status='new')` projection reused by the notify pipeline — one projection so the digest and the count can't drift. (9) **Telegram creds in `Settings` as `SecretStr | None`** (token) + `str | None` (chat_id), from `BEACON_TELEGRAM_*` env; **absent → `StdoutNotifier`** so ingest never fails unconfigured (SPEC's StdoutNotifier fallback). `SecretStr` keeps the token out of reprs/logs. (10) **View is a URL param** (`?view=searches`) not React state — so filters set on Jobs survive the hop to Saved searches (that's how "save from current filters" reads them) and the view is shareable; Companies/Countries nav items still deferred to slices 11/10. (11) **CourierNotifier stays deferred** behind the `Notifier` port (drop-in later), per PLAN. (12) **Acceptance gate `scripts/spot_check_digest.py`** (mirrors the slice-3/6/7 spot-checks): temp DB, seeds the acceptance scenario, sends live when `BEACON_TELEGRAM_*` are set else Stdout; proves one grouped message + dedup. Live phone send is the only step pending a real bot token.
- **2026-07-08** — **Slice 7 shipped (HN Who's Hiring + JobTech — company-less sources); decisions during the build:** (1) **Company-less topology is a new ingest mode, not a new ATS adapter.** Every prior source is one-company→one-board; HN/JobTech are one-source→many-companies, so they don't fit `ingest_all(companies, source_for)`. New use case `ingest_companyless_source(source, jobs, companies, classifier)` resolves-or-creates a shadow company **per posting**. The `JobSource` port is unchanged (both still `fetch()`/`normalize()`); the only domain change is `NormalizedJob` gaining an optional `company_name` (ATS jobs leave it None — their company comes from the seed being polled; company-less sources set it). Extracted the shared classify+sponsorship+upsert body into `_upsert_posting` (both use cases call it); `_resolve_sponsorship` now takes `registry_flags` not a Company. (2) **Shadow companies via `CompanyRepo.get_or_create` (`INSERT … ON CONFLICT (name) DO NOTHING`)** — a name that's already a real seed is **reused untouched**, so an HN posting at (e.g.) Anthropic inherits its `registry_flags` and resolves to `registry_inferred`, and its real `ats_type` isn't clobbered to `none`. New employers get a shadow row: `ats_type='none'`, `ats_slug=''`, `priority=5`. `SHADOW_ATS_TYPE='none'` is a named constant. (3) **The CLI filters `ats_type != 'none'` out of the ATS poll list** so shadow rows (which have no adapter) are never iterated/logged as `no_adapter` skips; dormant real seeds (smartrecruiters etc.) still surface that skip. `--source ID` (mutually exclusive with `--company SLUG`) runs one company-less source for acceptance. (4) **HN first-line parse is pure domain** (`domain/hn.py` `parse_hn_posting(text) -> HnPosting | None`): takes the first non-empty paragraph (HTML `<p>`/newline aware, reusing `normalize_description`), splits on `|`; needs ≥2 fields or returns None (skip). company=field 0; role = first field with a role keyword (else None → title falls back to company); location = first field resolving to a country or carrying remote/hybrid/onsite. Role/location keyword tables are DATA; spot-check misses extend them. (5) **HN concurrency vs politeness tension, recorded honestly:** the adapter fans out item fetches under an `asyncio.Semaphore` (PLAN's bounded concurrency), but `PoliteClient` holds a per-host lock across the whole round-trip, so with the shared client HN item fetches are effectively serial (1 rps). The real win is the **per-thread unseen-kids cache** (in-memory `set`, reset on a new thread id) making re-polls cheap; the Semaphore is a correct upper bound that a future Firebase-friendly fetcher would activate. Live acceptance used a dedicated `min_interval=0` client (HN's Firebase is a burst-tolerant CDN) → 223 kids in ~30s. (6) **Kid-fetch resilience:** each item fetch is isolated (`(id, item, ok)`); a kid the API answered (even a purged `null`) is cached so it isn't re-fetched, but a kid whose fetch **raised** stays uncached and retries next poll. Deleted/dead comments and replies with no parseable header are dropped. (7) **HN company field strips trailing parentheticals** (`(YC W22)`, `( https://… )`) — a live-spot-check finding: otherwise one employer is shadowed under several names across months; a name that is *only* a parenthetical is kept intact. (8) **JobTech `country_code` is a numeric taxonomy, NOT ISO-2** (live acceptance bug: was storing `'199'`). Map `199 → SE`; absent → SE (the board default, per PLAN "SE country default"); any other (foreign) numeric code → `None` rather than a fabricated ISO (location_raw preserved). Naive `publication_date` (the API omits offsets) is interpreted as `Europe/Stockholm` then converted to UTC (a bare `.astimezone` would fabricate from the host tz); `webpage_url` falls back to a reconstructed Platsbanken ad URL. (9) **Acceptance gate is `scripts/spot_check_hn.py`** (mirrors the slice-3/6 spot-check scripts; live network, never the suite); every genuine parse miss became a parametrized `test_hn.py` row. Fixtures recorded to real shapes under `tests/fixtures/{hn,jobtech}/`.
- **2026-07-08** — **Slice 6 shipped (explicit sponsorship text tiers); decisions during the build:** (1) **Text detection is a pure domain function** `detect_sponsorship(text) -> SponsorSignal | None` in `domain/sponsorship.py` (alongside `resolve_tier`), analogous to `domain/matching.py` — no IO, phrase/pattern tables are DATA. `SponsorSignal(tier, evidence)` is the one value object, reused for both the detected text signal and the resolved tier passed to the repo. (2) **`resolve_tier(text_tier, flags)` is still the single precedence point** (explicit > registry > unknown, no > yes) — unchanged from slice 2; ingest feeds it the detected text tier. Precedence at the DB level is preserved by the pre-existing `resolve_registry_tier` skipping `_EXPLICIT_TIERS`, so a later registry refresh never overwrites a text tier. (3) **Tier + evidence resolved at ingest time, gated by content_hash** exactly like classification: computed only on a new/changed hash, `None` on an unchanged re-poll → stored tier/evidence survive. Wired in `ingest_source` via `_resolve_sponsorship(job, company)` (reads `company.registry_flags`). (4) **`upsert` gained `sponsorship: SponsorSignal | None`**; the SQL uses named params so `:sponsor_tier` can drive both the INSERT (`COALESCE(:sponsor_tier,'unknown')`) and the UPDATE (`sponsor_tier = COALESCE(:sponsor_tier, jobs.sponsor_tier)`, `sponsor_evidence = CASE WHEN :sponsor_tier IS NOT NULL THEN :sponsor_evidence ELSE jobs.sponsor_evidence END`) — a supplied signal overwrites both (evidence NULL clears stale text), `None` keeps both. Decoupled from `excluded.*` so the insert-default and the keep-prior cases don't collide. No migration (`sponsor_evidence` existed since 001). (5) **NO uses regex, not fixed substrings** — negations put words between negator and verb ("not currently able to sponsor"), so `\bnot\b[^.!?]*\bsponsor` (sentence-scoped) is needed; a fixed "able to sponsor" substring read that as YES (real Anthropic miss). NO is scanned across all sentences before YES ("no beats yes"). (6) **YES also regex** — offers are phrased loosely ("relocation and family support are offered", "relocation ... provided"); `\brelocation\b[^.!?]*\b(package|…|provided|offered)\b` + the reverse verb-first form. **Relocation is treated as a positive signal** per the PLAN yes-list ("relocation package"); the offering *verb* is what separates an offer from a mere requirement ("it will require a relocation …" stays silent). (7) **Evidence is clipped to a ±80-char window around the match** (`_EVIDENCE_RADIUS`), not the whole sentence — because `normalize_description` collapses newlines, HTML bullet lists become one 1000+ char run-on "sentence"; the clip yields a readable highlight for the slice-10 drawer without touching the hash-gating normalizer. (8) **API/UI scope: detail contract only** — `sponsor_evidence` added to the `JobDetail` read model + `JobDetailOut` DTO + `types.ts` (`JobDetail`), not the list DTO (the tier badge already carries the list signal); the drawer that renders the highlighted sentence stays deferred to slice 10, consistent with the slice-5/5.5 UI-defer precedent. (9) **Live spot-check tool** `scripts/spot_check_sponsorship.py` (mirrors the slice-3 classifier one) is the acceptance gate; every miss it found became a pattern-table entry + a parametrized test row (append, never delete).
- **2026-07-08** — **Slice 5.5 shipped (per-job user status); decisions during the build:** (1) **No migration** — `user_status TEXT NOT NULL DEFAULT 'new'` already existed in migration 001 (created ahead for this slice, like `canonical_id`). (2) **`UserStatus` StrEnum in `domain/status.py`** is the single source of truth for valid values (new/seen/hidden/starred) + `DEFAULT_VISIBLE = all − {hidden}`, mirroring the `SponsorTier` pattern; the API query/body params are typed against it so bad values 422 automatically (no hand-rolled validation). (3) **Reset-on-hash-change lives in the upsert SQL** as `user_status = CASE WHEN jobs.content_hash != excluded.content_hash THEN 'new' ELSE jobs.user_status END` — a materially edited posting is genuinely new again; an unchanged re-poll (or classification-only change) keeps the user's decision. Pure-SQL, no extra round-trip. (4) **Status is a property of the canonical row** (the one the list shows). `PATCH /jobs/{id}/status` resolves a duplicate id → its canonical before updating (same resolution as `get_job_detail`) and returns `{id: canonical_id, user_status}`; the `/jobs` `status` filter reads the canonical row's status (list already filters `canonical_id IS NULL`). (5) **`/jobs` `status` param semantics:** absent → default view = everything except `hidden`; `status=all` → no filter (includes hidden); a specific value → only that status. The UI's four views map: New→`status=new`, Starred→`status=starred`, **All→ no param (excludes hidden)**, Hidden→`status=hidden` — so the UI never sends `status=all` (that API affordance exists but is unused by this UI; DESIGN's "All" = all-but-hidden). (6) **Default UI view is New** (the morning scan — the slice's whole point); it's kept out of the URL for clean shared links, other views set `?status=`. (7) **`user_status` exposed on `/jobs` list + detail DTOs** (new read-model field) so the table can render the new-dot, star-fill and hidden-greying without an extra fetch. (8) **Live segment counts deferred to slice 10** — DESIGN §2 wants a live count in each segment (and the nav "Jobs" badge = new-count); implementing now would add 4 count queries and reorder the existing test suite's `fetch` calls. Deferred to the slice-10 nav/drawer UI pass, consistent with the drawer/tier-badge defers. (9) **Row keeps the external-link action** alongside the new star + hide/restore buttons (actions column widened 64→100px) — the "open original" affordance otherwise only returns with the slice-10 drawer CTA, and users need it in the interim.
- **2026-07-08** — **Slice 5 shipped (cross-source dedup); decisions during the build:** (1) **Match key = exact `(company_id, normalized_title, country)` AND simhash(description) within Hamming ≤ 8** (SPEC §5 key 2). The exact title+company+country tuple is the *primary* false-merge guard ("Senior iOS" vs "Senior Android" never collide because titles differ); the simhash only has to reject the rare generic-title collision ("Software Engineer" twice at one company for two unrelated roles). `normalize_title` is deliberately conservative — lowercase + whitespace-collapse only; punctuation differences keep titles distinct (under-merge > over-merge). (2) **simhash hand-rolled in `domain/dedup.py`** (pure, no dep — PLAN allowed a lib but stdlib-first + it's central to correctness): 64-bit Charikar simhash over lowercased alphanumeric tokens weighted by frequency, hashed with **blake2b (deterministic across processes)** not the salted builtin `hash()`. (3) **Threshold 8 chosen on evidence, not to pass a test:** measured on a realistic ~77-token JD — whitespace/punct-only diff = 0 bits, short footer = 1, added sentence = 1, a 15-word footer = 7; a different eng role = 22, a marketing role = 14. 8 sits in the wide 7↔14 gap, biased toward merge (showing the same job twice is the worse sin). **simhash is noisy on very short text** (a 5-word footer on a 28-token doc flips 11 bits) — tests therefore use realistic-length descriptions; the domain boundary test (`test_dedup.py`) documents the near/far separation. (4) **Canonical = lowest id in a cluster** (earliest-inserted, stable/deterministic); clustering is transitive union-find within each group. Canonical row keeps `canonical_id = NULL`; duplicates point at it. `assign_canonicals(rows) -> {id: canonical_id}` is **pure** (fast unit tests, no DB); the `dedupe_jobs` use case just applies the mapping via the repo. **Idempotent** — re-running yields the same links. (5) **Dedup runs as a post-upsert pass over the whole DB** (PLAN task wording), once after `ingest_all` — a Greenhouse job can't be compared to a Lever job until both are persisted. Wired into `beacon.ingest` (prints `dedup groups= duplicates=`). Zero changes to domain from the app side beyond the new pure module; `JobRepo` port gained `list_dedup_rows`/`set_canonical_links`/`get_job_detail`. (6) **`/jobs` list now filters `canonical_id IS NULL`** (canonical-only); existing rows default NULL so no behaviour change for undeduped data. (7) **`GET /jobs/{id}` detail** resolves a duplicate id up to its canonical and returns `description` + `duplicate_sources` (every underlying posting's source/company/url). (8) **No migration** — `canonical_id` column has existed since migration 001. (9) **Frontend scope: backend + contract-types only** — `JobDetail`/`DuplicateSource` mirrored in `types.ts`; the interactive detail *drawer* stays deferred to slice 10 where DESIGN.md owns it (consistent with the slice-2 decision to defer the drawer). The acceptance ("one row in UI") is met by the canonical-only list. *(User was away when asked to confirm this scope fork; chose the no-throwaway option and recorded it here — revisit if a minimal drawer is wanted sooner.)*
- **2026-07-08** — **Slice 4 shipped (Lever + Ashby adapters); decisions during the build:** (1) **`polite_get()` realised as a `PoliteClient` behind a new `Fetcher` port** (`application/ports.py`) rather than a bare function — every adapter now takes a `Fetcher` and calls `get_json(url, params=)`; the concrete `PoliteClient` (`adapters/http/polite.py`) owns 1 rps **per host** (per-host `asyncio.Lock` + monotonic throttle, so the budget is global across all Greenhouse boards on the shared host), conditional GET (ETag/If-Modified-Since → 304 served from an in-memory body cache), 3-try exponential backoff on `{429,500,502,503,504}` + transport errors, 15s timeout. Clock + sleep are injected so the suite never waits. **One instance is shared across all companies** (wired in the CLI). (2) **GreenhouseAdapter refactored onto `Fetcher` too** (was raw httpx) so all three adapters go through the one polite door; its tests build a real `PoliteClient(min_interval=0.0)` over a `MockTransport`. (3) **Factory is now a registry** `_ADAPTERS: dict[str, Callable[[str, Fetcher], JobSource]]`; `SUPPORTED_ATS` is derived from it (greenhouse/lever/ashby), and `test_all_adapters_satisfy_the_jobsource_protocol` parametrizes over it — new source = one dict entry. Zero changes to application/ or domain/ (the port abstraction held). (4) **Lever specifics:** bare JSON array (no envelope) at `api.lever.co/v0/postings/{slug}?mode=json`; `createdAt` is **epoch milliseconds** → `fromtimestamp(ms/1000, UTC)`; title=`text`, url=`hostedUrl`, description=`description` (HTML → `normalize_description`), location=`categories.location`. Lever hands an **ISO-2 `country`** directly — trusted when it's 2 alpha chars (`_iso2`), else falls back to `parse_location`; city still from `parse_location`. (5) **Ashby specifics:** `{"jobs":[...]}` envelope at `api.ashbyhq.com/posting-api/job-board/{slug}` (no params); `publishedAt` ISO-8601, `descriptionHtml`, `location` free string → `parse_location` (regions like "Europe" → no country, matching the conservative slice-1 policy). (6) **Fixtures** trimmed from live boards (`immutable` 3+1 edge, `linear` 5+1 edge) under `tests/fixtures/{lever,ashby}/`; each carries a hand-edited edge posting (null date; Lever also null country) mirroring the greenhouse edge-fixture precedent. Descriptions truncated to keep fixtures small.
- **2026-07-08** — **Slice 3 shipped (heuristic classifier); decisions during the build:** (1) **Category is read from the job TITLE only, not the description** — the biggest decision, forced by the acceptance spot-check: matching the body tags every posting at an AI company as `ai-ml` (their boilerplate constantly says "LLM/AI") and fintech bodies as `backend` ("go-to-market" → `go`), giving ~50% precision. The title names the role; ambiguous/empty residue is by design the slice-9 LLM's job. **Level** still reads title tokens for seniority + a years-of-experience regex over title+body (years are role-specific, not boilerplate). (2) **Removed bare `ai` and `go` keywords** (misfire on "AI Native" sales titles / "go-to-market"); use phrases (`ai engineer`, `ml engineer`, `ai/ml`) or the specific form (`golang`). (3) **`Classifier` is a port with `HeuristicClassifier` as a (pure) adapter** in `adapters/classify/` per SPEC §7 — it's grouped with its slice-9 LLM sibling under one port even though pure; the value objects `Category`/`Level`/`Classification` are domain. Keyword tables are DATA in `adapters/classify/keywords.py`. (4) **Classification cached by content_hash in the pipeline**: `ingest_source` gained a `Classifier` param and classifies only when `JobRepo.content_hash_for` differs; `upsert` gained an optional `classification` and uses `COALESCE(excluded.categories, jobs.categories)` so an unchanged re-poll keeps its prior classification. (5) **`categories` stored as a sorted comma-joined string** in the existing (nullable) `jobs.categories` column — **NULL = never classified** (backfill picks these up), **"" = classified, nothing matched** (honest empty, skipped). No migration needed (columns existed since 001). (6) **Backfill** = `backfill_classifications` use case + `python -m beacon.classify` CLI (`list_unclassified`/`set_classification` repo methods). (7) **`/jobs` category[]/level[] filters are opt-in** like tiers; category match is comma-delimited membership (`',' || categories || ','` LIKE `%,ios,%`) so a filter for `ios` hits multi-label `ai-ml,ios` without substring bugs. (8) **Frontend**: category/level are DESIGN §2 row-2 pill toggles (URL params `category`/`level`); JobTable gains Category chips + a mono uppercased Level column per DESIGN §tokens; shared `taxonomy.ts` gives FilterBar pills and JobTable chips one label source; added token `--text-chip: #4c5964` (DESIGN category-chip text). (9) **Acceptance spot-check tool is live** (`scripts/spot_check_classifier.py`, 4 verified Greenhouse boards, `--all`/eng-only) — network is fine for manual acceptance, never the suite; every miss it found (space-form "back end"/"front end", java/infra/sre/devops/systems-engineer/javascript/aosp) became a keyword-table row + a parametrized test row.
- **2026-07-08** — **Fresh-clone gap fixed at session start:** frontend `node_modules` was missing `@eslint/js` (stale install) so `make verify` failed on the frontend leg; `npm install` restored it. Not a code change — noted so a future clone runs `npm install` before first verify.

- **2026-07-07** — **Slice 2 shipped (the moat); decisions/deviations during the build:** (1) **`match_evidence TEXT` column added to `companies` (migration 002)** — SPEC §7's schema omits an evidence column, but SPEC §5.3 / PLAN slice 2 require the MANUAL flag to carry an evidence note + date, and the slice-10 drawer will show which registries matched. One free-text column serves both the fuzzy audit trail (`"UK Skilled Worker; NL KvK …; US 2 certified LCA filings"`) and MANUAL notes (`"MANUAL: listed on relocate.me (flagged 2026-07-07)"`); the schema test was updated to match. (2) **Matching rule = distinctive-token-set EQUALITY, not overlap** (the "Cohere rule"): after dropping SUFFIX/GEO/STRUCTURAL token tables (data in `domain/matching.py`), the remaining sets must be equal — this is what rejects Cohere Health / Notion Capital / Stripe Consulting / Grab+Go while matching Cohere US / Notion Labs / Stripe Payments. STRUCTURAL deliberately includes divisional words seen in real subsidiaries (operations/software/payments/financial/technologies/labs/markets/…) but **excludes** consulting/capital/partners/investments/advisors (the trap generators). Confidence: 1.0 when suffix-only forms already match, 0.9 when geo/structural stripping was needed, None below. (3) **Tier resolution is owned by `refresh_registries`** (single `resolve_tier(text_tier, flags)` precedence fn + a bulk `JobRepo.resolve_registry_tier` that skips explicit-text tiers) — ingest is untouched this slice; a new job for a flagged company gets its tier on the next refresh. (4) **JobDetail "which registries matched + confidence" display deferred to slice 10** with the rest of the drawer (per PLAN UI build note) — slice-2 UI is the sort segmented control + opt-in tier dropdown on the Jobs view (badge already shipped in slice 1). (5) **`registries_meta` table created but not yet populated** — `fetched_at`/`row_count` writing lands in slice 11 alongside the staleness-nag reader that consumes it. (6) **Registry snapshots via `Settings` paths** (`data/registries/`, gitignored; env-overridable) — real exports dropped there for `python -m beacon.refresh`; only trimmed fixtures are committed and the spot-check script reads those. Missing snapshots are skipped, not fatal. (7) **MANUAL preservation:** `refresh_registries` OR-s fuzzy UK/NL/US bits but never clears a prior MANUAL bit; a MANUAL-only company with no fuzzy hit is left entirely intact. (8) `hypothesis` added to dev deps for the `registry_inferred ⇒ flags != 0` property test.
- **2026-07-04** — **Slice 1 shipped; deviations/policies set during the build:** (1) **`jobs.location_raw` column added** (not in SPEC §7 schema): ATS location strings that don't parse to a single country/city (`"Bangkok or Shanghai"`, `"North America (Remote)"`) keep country=city=NULL, and the raw string survives so a future better parser can re-parse without re-fetching. (2) **Location parsing is conservative by policy** — country names (+aliases), US state codes, and a region blocklist only; a bare city (`"Bangkok"`) stays country-less rather than fabricating TH via a city table (candidate improvement in slice 3's remote_scope work). (3) `RawPosting` is a `Mapping[str, Any]` type alias, not a wrapper dataclass. (4) `ingest_all` takes `Sequence[Company]` (caller lists/filters); adapter availability is a `SourceFactory` returning `None` for ATS types without adapters — SUPPORTED_ATS is implicitly the factory's keys ({greenhouse} today, lever/ashby in slice 4). (5) `sort_rank` SQL CASE is **generated from `domain/sponsorship.SORT_RANK`** so SQL can never disagree with the domain table. (6) **polite_get() not yet built** (plain 15s-timeout client) — required before slice 4 gives us multiple adapters and the scheduler polls at volume; tracked in Open items. (7) Greenhouse fixture note: `edge_locations.json` last job has a deliberately hand-nulled `first_published` (no recorded slug happened to omit it; normalize must map it to `posted_at=None`). (8) Frontend country dropdown ships without the P/☆ tier badges until the countries table exists (slice 10).
- **2026-07-04** — **Slice 0 shipped; two scaffold deviations from the pinned recipe:** (1) Makefile backend commands are prefixed `uv run` (pinned recipe assumed an activated venv; `uv run` auto-syncs, which is what makes "verify passes from clean clone" actually true) — frontend recipe unchanged; a `make setup` convenience target added. (2) The python conventions skill installed at `.claude/skills/modern-python-conventions/` (its frontmatter `name:` is `modern-python-conventions`, and skill dir must match frontmatter) — other three match their filenames. Also: `db.py` migration runner placed at `beacon/adapters/persistence/db.py` (sqlite is IO → adapters layer), migrations dir at `backend/migrations/` per CLAUDE.md; launchd plist stub at `deploy/com.beacon.scheduler.plist` (Disabled, unloaded).
- **2026-07-04** — **Pre-dev consistency pass, five corrections applied:** (1) seed ATS counts corrected everywhere to match the actual CSV — greenhouse **24**, lever **10**, ashby **11** (45 supported), **8** awaiting adapters across 5 types (smartrecruiters 3, workday 2, workable/gem/bendingspoons 1 each); SPEC §5.1, PLAN slice 11 seed line, DESIGN §3 updated, with a note that UI counts are computed from the companies table, never hardcoded; (2) stale `MigrationsverketRegistry` removed from SPEC §7 architecture tree (registry dropped 2026-07-04, tree missed); (3) PLAN slice-2 `test_registry_flags_bitmask` rewritten UK+NL → UK|NL — it still referenced the nonexistent SE bit; (4) DESIGN.md header note corrected: the zip's SPEC.md is byte-identical to canonical (verified by diff), not "an older snapshot"; (5) `uk_sponsors_fixture.csv` re-saved with CRLF line endings so the slice-2 "survives CRLF" test actually exercises the real register's format (was LF-only). Known gaps left open, tracked below: no ATS response fixtures yet (recorded after slice-1 task 0), and `frontend-design`/`typescript-conventions` skills referenced by PLAN's UI build note were never delivered.
- **2026-07-04** — **Design v2 update** — single change from v1: drawer registry-evidence section now explicitly specifies rendering the **MANUAL** flag (confidence 1.0) alongside UK/NL/US, not just the UK/NL the prototype samples. Folded into DESIGN.md. Bundled SPEC in the zip unchanged. No slice/schema impact — MANUAL was already in SPEC §5.3 and the bitmask; this just confirms the UI surfaces it.
- **2026-07-04** — **Design freeze DONE** (supersedes earlier "deferred"). Claude Design delivered high-fidelity UI: theme "Nordic Slate & Teal", four views (Jobs / Saved searches / Companies-health / Countries+world-map) + job-detail drawer, full token tables. Adopted as **DESIGN.md** (canonical UI source of truth; SPEC.md wins on any product conflict). PLAN gains a "UI build note"; Countries view+map folded into slice 10, Companies-health view into slice 11; CLAUDE.md frontend conventions now point at DESIGN.md tokens. Note: the SPEC.md copy bundled in the design zip is an older snapshot — canonical SPEC unchanged and authoritative.
- **2026-07-04** — **Pre-finalization review, six items resolved:** (1) slice 1 gains a manual slug-verification task 0 (hit greenhouse endpoint in browser before coding); (2) no LLM in slice 9 tests — `LLMClassifier` behind `Classifier` port, `FakeLLMClassifier` in suite; (3) **time: UTC storage, Asia/Jakarta day boundaries** for posted_since + digest (one `LOCAL_TZ` constant at edges); (4) `registry_flags` bitmask confirmed **UK|NL|US|MANUAL** (no SE bit); (5) saved-search digest lines record **match_reason** (which search + filters fired), `seen_matches.match_reason`; (6) **per-job user_status** (new/seen/hidden/starred) added as **slice 5.5** — status preserved across re-poll unless content_hash changes. All four registries + application-tracking still deferred per scope.
- **2026-07-04** — **SE registry correction (user-verified):** Migrationsverket's employer certification scheme was discontinued Dec 2023 — no Swedish sponsor register exists (Sweden has no employer licensing; any compliant employer may sponsor, so SE `unknown` companies skew sponsor-likely). `MigrationsverketRegistry` dropped from slice 2. **relocate.me & similar curated boards adopted as a MANUAL registry flag** — hand-entered with evidence + date, never scraped (their curation is their product); participates in `registry_inferred` at confidence 1.0. SPEC §4/§5.3, PLAN slice 2 updated.
- **2026-07-04** — **Source health & recovery added as slice 11** (SPEC §7 "Source health & recovery"): failure taxonomy (gone/unreachable/schema_drift), threshold-based quarantine, weekly restore probe, health in Telegram digest, re-slug recovery as data edit. **Latent bug fixed in the process:** closed-posting sweep now counts absence only on *successful* polls — a 404'd board can no longer mass-close its jobs (slice 10 test `test_failed_poll_never_closes_jobs`).
- **2026-07-04** — Nine open questions resolved: (1) **TelegramNotifier direct** (Bot API), CourierNotifier deferred behind the Notifier port; (2) name **Beacon final**; (3) `seeds/companies.csv` delivered — 53 verified companies, schema pinned `name,ats_type,ats_slug,country_hq,priority`; unsupported ATS types (smartrecruiters/workable/workday/gem/bendingspoons) load but stay dormant until adapters exist; (4) registry source URLs pinned in TODO; (5) countries verification via VERIFY-COUNTRIES.md checklist; (6) **design freeze deferred** — UI slices proceed on utilitarian defaults; Claude Design freeze before UI-polish work; (7) **localhost/Tailscale only, no auth in MVP** — public exposure requires an auth slice first (SPEC §9); (8) `make verify` recipe pinned in PLAN slice 0 (ruff+mypy+pytest / eslint+tsc+vitest); (9) HN adapter uses the **official Firebase API** (whoishiring user → latest thread → kids), not Algolia.
- **2026-07-04** — Principles reordered: **Clean Architecture is the primary principle** (SPEC guiding principles, CLAUDE.md rule 1); TDD loop made explicit as red → green → **mandatory refactor checkpoint** with a trigger table (PLAN.md "TDD loop"); layer-leak triggers outrank all other refactor triggers.
- **2026-07-04** — Sponsorship changed from hard requirement to **soft signal**: badge + default sort (`sort_rank DESC, posted_at DESC`), tier filter strictly opt-in, `explicit_no` shown last but never hidden. SPEC §2/§3/§7/§10, PLAN slice 2, CLAUDE.md updated.
- **2026-07-04** — Spec suite created. Working name: Beacon. Scope locked to ATS APIs + API/RSS boards; LinkedIn/Indeed and application tracking explicitly deferred (SPEC §2).
- **2026-07-04** — Sponsorship tier precedence fixed as pure function: explicit_no > explicit_yes > registry_inferred > unknown.
- **2026-07-04** — Country visa reference data (SPEC §4) is knowledge-as-of Jan 2026; every row carries `verified_at` + `source_url`. Re-verify SE citizenship reform and CA Express Entry draws against official sources before weighting those countries in strategy.

## Open items / TODO (not slice work)

- [x] ~~Build initial `seeds/companies.csv`~~ — delivered 2026-07-04, 53 verified rows. Place at `seeds/companies.csv` in repo.
- [x] ~~Download registry snapshots for fixtures~~ — **ALL FOUR RESOLVED 2026-07-04:**
  - [x] UK sponsor register CSV — delivered (2026-07-03 snapshot, 142k rows) → `uk_sponsors_fixture.csv` (35 rows)
  - [x] NL IND recognised sponsors — delivered via DevTools extraction (12,886 orgs + KvK) → `ind_sponsors_fixture.csv` (25 rows)
  - [x] SE Migrationsverket — does not exist (scheme discontinued Dec 2023); MANUAL flag workflow instead
  - [x] US H-1B LCA — delivered (FY2026 Q2 XLSX, 1.04M rows / ~210k filings, 31,587 employers) → `h1b_lca_fixture.csv` (40 rows incl. Denied + padding-row cases). ~25 seed companies confirmed US sponsors; 10 confirmed absent (negative controls). Cohere US vs Cohere Health matcher rule + DBA matching added to slice 2.
  - Fixture destination in repo: `backend/tests/fixtures/registries/`
- [x] ~~Build `polite_get()` (1 rps/host, ETag/If-Modified-Since, 3-try backoff, 15s timeout)~~ — **done 2026-07-08 (slice 4)** as `PoliteClient` behind the `Fetcher` port (`adapters/http/polite.py`); all adapters route through one shared instance.
- [ ] First MANUAL curation pass: browse relocate.me / swedishtechjobs for SE+NL companies; flag matches in seed list, add promising new companies as seed rows (after slice 2 ships the flag CLI)
- [x] ~~Decide final project name~~ — **Beacon**, final.
- [x] ~~Confirm notifier contract~~ — Telegram Bot API direct; need bot token + chat_id in `.env` before slice 8 (fresh bot via BotFather, or reuse Relay's).
- [ ] Work through VERIFY-COUNTRIES.md — confirm SPEC §4 rows against official sources, update `verified_at`/`source_url` (blocks strategy decisions, not build slices).
- [x] ~~Claude Design freeze session~~ — **done 2026-07-04**, delivered as DESIGN.md (Nordic Slate & Teal). Prototype `.dc.html` files kept as reference in the design bundle.
- [ ] Future adapters from dormant seed rows: smartrecruiters (3 companies), workable (1), workday (2), gem (1), bendingspoons (1).
- [ ] Missing skills referenced by PLAN "UI build note": `frontend-design` and `typescript-conventions` were never delivered (only react/python/fastapi/pytest exist). Deliver or drop the references before frontend slices. ~~Must be installed at `.claude/skills/<name>/SKILL.md`~~ — **installed 2026-07-04** (python one lives at `modern-python-conventions/` matching its frontmatter name); the four still lack their `references/*.md` companions.
- [ ] Record ATS response fixtures (Greenhouse first) after slice-1 task 0 slug verification → `backend/tests/fixtures/greenhouse/` etc. Only registry fixtures exist today.
- [x] ~~On repo creation (slice 0)~~ — done 2026-07-04: `seeds/companies.csv` placed; 3 registry fixture CSVs at `backend/tests/fixtures/registries/` (UK CRLF endings verified intact); `.gitignore` covers `.DS_Store` + design zip (kept locally, not shipped).

## Session log

*(newest first)*

- **2026-07-09** — **Slice 11 built + suite-verified + live-accepted (source health & recovery).** TDD chain, committed in green sub-slices: (1) `domain/health.py` `SourceHealth` state machine + 7 table tests; (2) `SourceUnavailable` at the `PoliteClient` door (httpx→FailureKind) + 3 polite tests; (3) `SqliteCompanyRepo` health persistence + reslug-resets-health + `list_quarantined` + 5 integration tests; (4) failure taxonomy in `ingest_source` (returns kind, never sweeps on failure) + `ingest_all` health filtering/recording + quarantine/freeze/restore tests; (5) `probe_quarantined` weekly restore probe + 3 integration tests; (6) `registries_meta` write in refresh + `stale_registries` (45d) + repo + 5 tests; (7) health in the digest (`HealthAlert`/`RegistryStale`, `build_health_alerts`, wired into `match_saved_searches`+`run_ingest`) + digest/report tests; (8) `GET /companies/health` + `get_company_health` (pending status, summary) + 2 API tests; (9) scheduler weekly probe job + `run_probe`; (10) frontend Companies view (summary cards + seed line + health table, `tokens.css` health colors) + 5 component tests + nav item. No new migration (health columns exist since 001). **Live acceptance** via new `scripts/spot_check_health.py` against real Greenhouse: nonsense slug → 404 → degraded/degraded/quarantined(gone) over 3 polls, pre-existing job's `closed_at` stayed null, digest health section listed it; fix slug → re-seed reset health to ok → recovery poll upserted **15** real Tines jobs; `/companies/health` summary reflected it. All three PLAN acceptance boxes pass. Backend **475** tests, frontend **50**, `make verify` green. **Deferred (recorded):** per-job "source stale since" banner on Jobs + sidebar source-health footer + nav count badges.
- **2026-07-09** — **Slice 10 built + suite-verified (CountryPanel + RemoteOK/WWR + scheduler); reboot-resume pending a manual launchd load.** TDD chain, committed in six green sub-slices: (10a) `domain/visa.py` reference data + migration 006 `countries` + `CountryRepo`/`SqliteCountryRepo` + `GET /countries`, seeded at lifespan/CLI startup; (10b) extended `JobDetail` with `registries`+`match_confidence` (`registry_names` decodes the bitmask) + the job-detail drawer (DESIGN §2: header/star/hide/close, chip row, sponsorship-evidence card per tier, description, teal CountryPanel, sources+CTA), URL `?job` open, open-marks-seen; (10c) Countries view (DESIGN §4: cards grid + canvas world map + pin↔card cross-highlight) as a new nav view; (10d) RemoteOK (JSON) + WWR (RSS, `Fetcher.get_text`) company-less adapters; (10e) closed-posting sweep (migration 007 `consecutive_misses`, `sweep_absent_jobs`, wired to run only on successful polls — `test_failed_poll_never_closes_jobs`); (10f) APScheduler wiring (`build_scheduler` + `python -m beacon.scheduler`) + fixed launchd plist, `run_ingest` poll toggles; (10g) `backup_database` (sqlite online-backup) + `scripts/backup_db.py`, scheduler nightly job. **Live-verified** a real uvicorn on a temp DB: migrations 001–007 apply on lifespan, `/healthz` ok, `/countries` returns 11 rows (Sweden's discontinued-register note + 8yr-reform caveat verbatim, primary-tier first); scheduler start smoke-tested (4 jobs, correct Asia/Jakarta next-run times). Backend **435** tests, frontend **45**, `make verify` green. **Pending (recorded):** reboot-resume — `cp` the plist to `~/Library/LaunchAgents` + `launchctl load`, then reboot (only you can do this on your box). Two earlier credentialed live steps (slice-8 Telegram send, slice-9 Anthropic spot-check) still open.
- **2026-07-09** — **Slice 9 built + suite-verified (LLM fallback classifier); live acceptance pending an Anthropic key.** TDD chain, green + committed per step: (1) domain `Classification.is_ambiguous` (= empty categories) + 5 table rows; (2) `LLMClassifier` (sync httpx → Anthropic `/v1/messages`, JSON-only prompt, tolerant parser) + 6 fixture-driven `MockTransport` tests, fixtures under `tests/fixtures/anthropic/`; (3) `TieredClassifier` (heuristic → ambiguous+budget → LLM, all-errors→heuristic, level-preservation) + `LLMBudget` port + 6 unit tests with `FakeLLMClassifier`/`FakeBudget` over the real heuristic; (4) migration 005 `llm_usage` + `SqliteLLMBudget` (LOCAL_TZ month key, injected clock, counts attempts, `calls_this_month`) + schema test + 4 integration tests; (5) `Settings` LLM fields + `make_classifier` factory + 4 tests; (6) `upgrade_ambiguous_classifications` (+ `JobRepo.list_ambiguous`) for the `''` residue backlog + 2 integration tests, both CLIs wired onto `make_classifier` (`beacon.classify --upgrade-residue`; both print `llm_calls_this_month`), `scripts/spot_check_llm.py` acceptance gate. **Smoke-verified** the CLI wiring end-to-end on a temp DB (heuristic-only, no key): migration 005 applies, `backfill classified=0 llm_calls_this_month=0`, `--upgrade-residue` runs. Backend **380** tests, frontend **33**, `make verify` green. **Deferred/pending (recorded):** the live spot-check (needs `BEACON_ANTHROPIC_API_KEY`) is the one acceptance step I can't run — set the key and run `scripts/spot_check_llm.py`, then `beacon.classify --upgrade-residue` to upgrade the backlog.
- **2026-07-09** — **Settings UI shipped: user can set Telegram creds + fire a live test send from the browser (slice-8 extension).** TDD chain, green throughout: (1) domain `TelegramConfig` value object (pure, token kept out of reprs, `is_configured`/`merge`) + 4 unit tests; (2) migration `004_app_settings.sql` (k/v table) + schema test; (3) `SqliteSettingsRepo` behind a new `SettingsRepo` port (4 integration tests: empty/roundtrip/overwrite/None-clears); (4) `application/settings.py` use cases — `effective_telegram_config` (DB merged over env), `update_telegram_config` (keep/clear/set token policy), `send_test_message` (5 unit tests); (5) shared `adapters/notify/factory.make_notifier`, refactored `beacon.ingest` onto it + DB-aware cred resolution; (6) `Settings.telegram_config()` env-bridge; (7) `api/settings.py` — `GET`/`PUT /settings/telegram` (token write-only) + `POST /settings/telegram/test` (real send, `httpx.HTTPError`→400 with reason) + `get_http_client` dep (test seam) (7 API tests incl. network-failure→400); (8) frontend `api/settings.ts` + `types.ts` + `SettingsPage` (uncontrolled form keyed on loaded settings, Save + Send-test with channel/error feedback) + third nav view in `App` (6 SettingsPage tests + 1 App nav test). **Live acceptance** (real uvicorn on a temp DB): migration 004 applied on lifespan; GET empty → PUT creds → GET masked (no token leak) → chat_id-only PUT keeps token (verified in the sqlite row); test-send with no creds → `channel:stdout` (digest printed to server log); test-send with bogus creds → real `api.telegram.org` call → `401 Unauthorized` surfaced as a clean 400. Backend **352**, frontend **33**, `make verify` green. **Only remaining slice-8 step:** the user pastes a real BotFather token + chat_id in Settings and clicks Send test message.
- **2026-07-08** — **Slice 8 built + locally verified (saved searches + Telegram digest); live phone send pending a bot token.** TDD chain, committed in green steps: (1) domain `saved_search.py` — `SearchFilters` + JSON round-trip + `match_reason`, and `digest.py` — `Digest`/`DigestGroup`/`DigestLine` + pure `build_messages` chunker (8 unit tests incl. grouping + ≤max split without breaking an entry); (2) `SavedSearch` entity + `SearchRepo`/`Notifier` ports + migration 003 (`saved_searches` + `seen_matches`) + `SqliteSearchRepo` (6 integration tests: create/get round-trip, list, delete, seen-ledger read-back, idempotent record, last_run) + 2 schema tests; (3) `match_saved_searches` use case — subtract seen → grouped digest → send-before-record → touch last_run (5 unit tests incl. match-only-new, reason recorded, empty-not-sent, send-failure-doesn't-record); (4) `TelegramNotifier` (Bot API sendMessage, plain text, 4096-split) + `StdoutNotifier` (5 adapter tests via httpx MockTransport: payload shape, chat_id, no parse_mode, multi-post split, error raises); (5) `/searches` CRUD API + `SearchSummary` "N new" count query + shared `to_job_filters` projection (5 API tests); (6) `Settings` Telegram creds (`SecretStr`) + wired `match_saved_searches` into `beacon.ingest` after dedup (StdoutNotifier fallback); (7) frontend — `SavedSearchesPage` (cards: name, N-new/up-to-date pill, mono filter summary, channel, last-run, delete; dashed "save from current filters" form) + nav view-switch in `App` (6 new frontend tests). **Local acceptance** via new `scripts/spot_check_digest.py`: acceptance search "senior iOS, SE+NL+IE, tier≥registry_inferred" + 2 matching iOS/SE+NL/registry_inferred jobs (US/backend control excluded) → ONE grouped digest; re-run → 0 (seen dedup). Backend **329** tests, frontend **26**, `make verify` green. **Pending:** fire a real Telegram message (`BEACON_TELEGRAM_BOT_TOKEN`/`CHAT_ID` + re-run the spot-check) — the only slice-8 acceptance step that needs a credential I don't have. **Deferred (recorded):** CourierNotifier (behind the port); Companies/Countries nav items (slices 11/10); nav count badges.
- **2026-07-08** — **Slice 7 built, verified, acceptance passed (HN Who's Hiring + JobTech — company-less sources).** TDD chain, committed in green steps: (1) domain `parse_hn_posting` (pure, first-paragraph/`|` header heuristic, role/location keyword tables as data), 17 table rows; (2) `NormalizedJob.company_name` + `ingest_companyless_source` + `CompanyRepo.get_or_create` (shadow employers; existing seed reused → inherits registry flags), shared `_upsert_posting` refactor, 4 unit tests + `FakeCompanyRepo`; (3) `HNAdapter` over the Firebase API (whoishiring → latest "Who is hiring?" thread → kids, `asyncio.Semaphore` fan-out, per-thread unseen-kids cache, `(id,item,ok)` resilience, deleted/dead/null/non-posting drops), 6 tests incl. bounded-concurrency probe + retry; (4) `JobTechAdapter` (SE default, numeric-country-code map, naive-date → Europe/Stockholm → UTC, reconstructed ad URL), 5 tests; (5) `factory.make_companyless_sources` + CLI wiring (`--source`, shadow rows filtered from the ATS poll). **Live acceptance:** `scripts/spot_check_hn.py` over the **July 2026 thread (274 kids)** — 76/80 sampled parsed cleanly (~2% junk, e.g. a poster leading with a location); full HN→DB ingest = 223 postings, 0 errors, 222 shadow companies, classified (backend/fullstack/frontend) + deduped. JobTech company-less pipeline live into a temp DB: 100 hits → shadow companies, 99 SE / 1 foreign→None, canonical `/jobs` rows correct. **Two acceptance-driven fixes:** JobTech `country_code` is numeric (199=SE) not ISO-2 (was storing '199'); HN company field strips trailing `(YC …)`/URL annotations. Backend 298 tests, frontend 20, `make verify` green. **Deferred (recorded):** HN first-fetch is 1 rps under the shared `PoliteClient` (per-host lock serialises) — the per-thread cache keeps re-polls cheap; a Firebase-friendly fetcher is the fix if first-fetch latency bites.
- **2026-07-08** — **Slice 6 built, verified, acceptance passed (explicit sponsorship text tiers).** TDD chain, committed in green steps: (1) domain `detect_sponsorship` + `SponsorSignal` (pure, phrase/pattern tables as data, NO-before-YES precedence, evidence = deciding sentence), parametrized table; (2) `upsert(sponsorship=)` — named-param SQL writes/keeps `sponsor_tier`+`sponsor_evidence` in lockstep with the classification-cache rule, 4 integration tests; (3) `ingest_source` resolves text-tier + registry via `resolve_tier` and passes a `SponsorSignal` (gated by content_hash), 4 unit tests incl. explicit-beats-registry; (4) `sponsor_evidence` on `JobDetail` read model + `JobDetailOut` DTO + `types.ts`, 2 API tests. **Live acceptance** via new `scripts/spot_check_sponsorship.py` over ~530 real postings (Anthropic/Adyen/Agoda/Tines): two genuine misses found and fixed (→ NO regex robust to "not currently able to sponsor"; → YES catches loose "relocation provided/offered"), after which precision is effectively 100% on inspected rows; all remaining silences verified true negatives. **Full pipeline live-checked** on a temp DB: `python -m beacon.ingest --company anthropic` → 399 rows tiered (388 explicit_yes "We do sponsor visas!", 11 explicit_no work-auth), `/jobs/{id}` returns a clipped readable `sponsor_evidence`, unchanged re-poll preserved the distribution. **Evidence clipped to ±80 chars** around the match (normalize collapses newlines → run-on "sentences"). Backend 265 tests, frontend 20, `make verify` green.
- **2026-07-08** — **Slice 5.5 built, verified, acceptance passed (per-job user status).** TDD chain, committed in green steps: (1) `UserStatus` domain enum + upsert reset-on-hash-change (`CASE` in ON CONFLICT), 3 integration tests (defaults new / preserves on unchanged / resets on changed hash); (2) `/jobs` `status` filter (JobFilters + repo + typed route param) — default excludes hidden, `all`/`new`/`starred` views, bad value 422, 5 API tests; (3) `PATCH /jobs/{id}/status` (repo `set_user_status` resolves duplicate→canonical, query, route with Pydantic `UserStatus` body) — 422 bad value / 404 unknown / lands on canonical, 4 API tests; (4a) `user_status` exposed on list + detail DTOs; (4b) frontend — `StatusTabs` segmented control (New/Starred/All/Hidden, default New), per-row Star + Hide/Restore buttons wired to a `useMutation` PATCH with `['jobs']` invalidation, new-dot on role cell, hidden-row greying, per-view empty states; 6 new frontend tests + `FakeJobRepo` given the new port method. **Live acceptance (real server on temp DB, 2 seeded jobs):** PATCH→seen dropped it from `status=new` (2→1) but `status=all` kept it; PATCH→hidden vanished from default view, recoverable via `status=all`; unchanged-hash re-upsert kept `seen`, edited-hash re-upsert reset to `new`; bad status 422 (list + patch), unknown id 404. Backend 233 tests, frontend 20, `make verify` green. **Deferred (recorded):** live segment counts → slice-10 nav/drawer pass.
- **2026-07-08** — **Slice 5 built, verified, acceptance passed (cross-source dedup).** TDD chain, committed in 4 green steps: (1) domain `dedup.py` — `simhash`/`hamming`/`normalize_title`/`assign_canonicals` (pure, union-find, canonical = lowest id), 11 table-driven unit tests incl. no-false-merge on distinct titles + description divergence + cross-company/country; (2) `dedupe_jobs` use case + `JobRepo` dedup/detail methods, `/jobs` list filters `canonical_id IS NULL`, 6 integration tests; (3) `GET /jobs/{id}` detail (resolves duplicate→canonical, lists every source) + 3 API tests; (4) dedup pass wired into `beacon.ingest` pipeline; frontend `JobDetail`/`DuplicateSource` contract types (drawer UI deferred to slice 10). **Threshold 8 set on measured evidence** (near-dup 0–7 bits, different role 14–22 on realistic JDs) — see Decisions. **Acceptance (live):** ingested `immutable` (lever, 3 real roles → dedup 0/0, no false merges), then cloned one posting under a second `source_id` with a reformatted+footered description → dedup collapsed it (list 3→3, groups=1, duplicates=1), `/jobs/{id}` detail listed both sources (lever + remoteok). Backend 220 tests, frontend 15, `make verify` green.
- **2026-07-08** — **Slice 4 built, verified, acceptance passed (Lever + Ashby adapters).** Built the shared `polite_get()` first (Open item cleared): `Fetcher` port + `PoliteClient` (1 rps/host, conditional GET, backoff; injected clock — 5 tests). Refactored Greenhouse + factory + CLI onto it. Added `LeverAdapter` (epoch-ms dates, ISO country field, bare-array API) and `AshbyAdapter` (jobs envelope, ISO dates), each with fixture normalize + fetch-endpoint tests from trimmed live boards; factory became a registry with `test_all_adapters_satisfy_the_jobsource_protocol`. Zero changes to application/domain — the `JobSource` port held. **Acceptance (live):** `python -m beacon.ingest --company immutable` → lever 3/3, `--company linear` → ashby 25/25, 0 errors; all 28 rows classified + dated, "Senior/Staff Fullstack Engineer" → fullstack/staff. Backend 200 tests, frontend 15, `make verify` green. **Open item cleared:** `polite_get()` built.
- **2026-07-08** — **Slice 3 built, verified, acceptance passed (heuristic classifier).** TDD chain, committed in 5 green steps: (1) domain `Category`/`Level`/`Classification` + `format/parse_categories`, `HeuristicClassifier` (word-boundary keyword match, most-senior-wins level, years regex) + `Classifier` port, table-driven tests; (2) pipeline classify hook — `ingest_source` classifies only on new/changed `content_hash` (`content_hash_for` + `upsert(classification=)` with COALESCE), `test_classification_cached_by_content_hash`; (3) `backfill_classifications` + `beacon.classify` CLI (`list_unclassified`/`set_classification`); (4) `/jobs` opt-in `category[]`/`level[]` filters + `categories`/`level` in DTO/read model; (5) FilterBar row-2 category/level pill toggles + JobTable category chips/level column, shared `taxonomy.ts`, `--text-chip` token. **Acceptance:** `scripts/spot_check_classifier.py` run live over anthropic/adyen/agoda/tines, 32 all-roles + 32 eng-only — **0 category misclassifications**, level clean; the empties left are genuinely title-ambiguous (Research Engineer, Data Scientist, generic SWE, security/support/solutions eng) → residue for the slice-9 LLM. **Key correction the spot-check forced:** category now reads the **title only** (body copy is boilerplate-contaminated; matching it gave ~50% precision at AI/fintech companies). Every miss became a keyword row + test row. Backend 184 tests, frontend 15, `make verify` green.
- **2026-07-07** — **Slice 2 built, verified, acceptance passed (the moat).** TDD chain, committed in 6 green steps: (1) domain foundation — `Registry` IntFlag (UK|NL|US|MANUAL, no SE bit), `RegistryCompany`, `resolve_tier`/`tier_sort_rank`, `matching.py` normalizer+matcher (50 exhaustive table rows covering every UK/NL/US register hazard — suffix chaos, CRLF, trading-as/dba/parenthetical aliases, token-boundary vs substring, the Cohere problem, false-positive traps), migration 002; (2) `RegistryIngester` port + UK/IND/H1B ingesters (strip/dedupe-by-route, KvK evidence, certified-only + per-employer filing counts, embedded-dba + TRADE_NAME_DBA columns; shared `iter_rows` CSV contract); (3) `match_company` aggregator + `refresh_registries` + `flag_manual_sponsor` use cases, `match_evidence` column, `hypothesis` invariant + 10-company parametrized spot-check; (4) `/jobs` sort toggle (tier/date) + opt-in `sponsor_tier[]` filter; (5) frontend sort segmented control + sponsor-tier dropdown (opt-in, off by default) in URL state; (6) `beacon.refresh` CLI (+`--flag`) + `scripts/spot_check_registry.py`. **Manual acceptance:** spot-check eyeballed — Adyen/Spotify/Stripe/Databricks/Plaid → UK|NL|US, 1Password → UK via trading-as, Bird → NL via parenthetical, Miro → US via dba, Stripe US aggregates 2 filings; every trap rejected (Grab+Go, Cohere Health, Notion Capital, Linear Investments, Maxlinear, Redditch, Gradyent, Grabowsky); controls (Wealthsimple/Truecaller/Mentimeter) → 0. CLI smoke-tested end-to-end (refresh 53/32, MANUAL flag, missing-company error). Backend 144 tests, frontend 11, `make verify` green.
- **2026-07-04** — **Slice 1 built, verified, acceptance passed.** Task 0 first: 5 greenhouse slugs live-verified (agoda/adyen/anthropic/teampicnic/tines all 200); fixtures recorded and trimmed to `backend/tests/fixtures/greenhouse/` (`tines_jobs.json` 15 jobs, `edge_locations.json` 9 handpicked location-format hazards). TDD chain: `parse_location` (18-row table), `normalize_description`+`content_hash`, `GreenhouseAdapter` normalize/fetch (MockTransport), migration 001 (full SPEC §7 companies+jobs), `ingest_source`/`ingest_all` (bad posting never kills poll; dead board never kills run), seeds parser (53-row real-file guard), `GET /jobs` (q/country multi/posted_since/limit/offset; default order from domain SORT_RANK; no default tier filter — test-guarded), frontend JobsPage (FilterBar keyword+country dropdown, JobTable with tier chips, tokens.css from DESIGN tables, URL-param filter state, TanStack Query). Acceptance: live `--company tines` ingest → 15 rows; re-run → 15 rows, `last_seen_at` bumped on all; filters exercised against real data through the vite proxy (UI verified by component tests + proxy smoke — worth a quick visual eyeball next session). Backend 53 tests, frontend 7, `make verify` green. Fixed test-isolation gap: explicit `cleanup()` in setupTests (vitest runs without globals).
- **2026-07-04** — **Slice 0 built and verified.** Backend: uv-managed Python 3.12, FastAPI app factory with `/healthz` (TDD: red → green), `db.py` migration runner (numbered `.sql`, `schema_migrations` bookkeeping, idempotent — 2 tests), ruff (line 100) + mypy strict + pytest-asyncio all green. Frontend: Vite + React 19 + TS strict (single tsconfig so `npx tsc --noEmit` really typechecks), vitest + testing-library (red → green on App render), eslint flat config. `make verify` green end-to-end. Repo-creation TODOs done: seeds, registry fixtures, skills installed, `.gitignore`, launchd stub. Next: slice 1 task 0 (verify greenhouse slugs, record fixtures).
- **2026-07-04** — Pre-dev readiness check of the full bundle; five doc/fixture corrections applied (see Decisions). Suite is consistent and ready for slice 0. No code yet.
- **2026-07-04** — Spec suite generated (SPEC/PLAN/CLAUDE/PROGRESS). No code yet.
